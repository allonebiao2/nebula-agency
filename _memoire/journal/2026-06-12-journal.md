# Journal — 2026-06-12

## Vendora Support — 2e pilier (LIVRÉ + déployé)
Idée de Mongazi : un agent de SUPPORT client (pas que vendeur) pour tout business avec site/SaaS/vitrine (pilote Abakar / WE ACT). Spec : `_specs/vendora-support-v1.md`. Construit en vagues testées, **additif et inerte par défaut** (`agent_role='vendeur'`), zéro régression sur le Vendora vendeur.

Livré (tout déployé sur Railway, prod saine 200) :
- **Lot 1** — DB : `bia_merchants.agent_role`/`kb_text`/`kb_instructions` + tables `bia_knowledge` & `bia_support_tickets`. Migration Supabase appliquée.
- **Lot 2** — `core/support_agent.py` : moteur support grounded + escalade-si-doute (outil `escalader`), base injectée + cachée. Testé en réel (répond depuis KB, escalade hors-KB sans inventer).
- **Lot 3** — back-office `boutique.html` onglet « Mode Support » (rôle + base de connaissances + consignes + tickets).
- **Lot 4** — routage : si `agent_role=='support'`, le handler entrant route vers `support_agent` ; escalade → `create_support_ticket` + `_escalation_notifier`.
- **Lot 5** — import PDF (pypdf → `bia_knowledge`) : endpoints + UI.
- **Lot 6** — **widget site** : `/widget.js?code=` (bouton lumineux « Discuter avec nous » + panneau stylé aux couleurs marque) + `/api/support/chat` (CORS) + ligne à copier dans le back-office.
- **Lot 7** — `core/support_report.py` : rapport (récurrents + corrections suggérées + résumé visiteurs) via modèle writer.
- **#1** support à l'inscription (choix Vendeur/Support + FAQ avant paiement).
- **#2** lecture auto du site : `core/site_reader.py` (httpx + parser stdlib) → endpoint `/support/site` « Lire mon site ».
- **#3** le widget logge les visiteurs (id localStorage → `bia_messages`) → résumés dans le rapport.
- **#4** gating : base support pour tous ; premium (widget/PDF/site/rapport) = Business/Empire (`_support_premium_ok`).

Commits : 16347f4 (spec+fondation), 155f542 (routage), 6eda9ce (back-office), f6aaa18 (widget), d94e6fb (PDF+rapport), a23b747 (site+visiteurs), f4dea97 (onboarding+gating), 60cbd74 (kits regroupés).

## Dispatch cerveau
- `core/support.py` `_VENDORA_FACTS` enrichi du mode support (les agents qui parlent de Vendora le connaissent + futurs hériteront).
- `_memoire/vendora/capacites.md` + `decisions.md` mis à jour.

## Livrables (dossier `_kits/`)
2 kits de vente (Vendora, NEBULA) + 2 présentations (Plan du Million, Pitch investisseurs) — PDF + HTML animé. Liens de téléchargement GitHub raw. Doc Drive « Téléchargements ».

## Reste (phase 2)
RAG (grosse base), tracking des visiteurs passifs (page views), canaux Messenger/IG/email en support, agent qui vend ET supporte en même temps.
