# Obsidian — état local de l'interface, à ne pas versionner
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json

# OS
Thumbs.db
.DS_Store

# Editor
.vscode/
*.swp

# Claude Code — config locale machine, skills installés
.claude/
.agents/
skills-lock.json
skill claude code/

# Secrets — clés API, jamais sur GitHub
.env
.env.local
*.key
secrets/

# Captures d'écran locales — ne pas versionner
_tmp_screenshots/

# Cloudflare Wrangler — cache de build local
.wrangler/

# Archives locales (ex. vidage de _partage/) — gros médias, hors GitHub
_archives/

# _partage/ — médias bruts déjà versionnés ailleurs sous leur forme finale.
# On garde le fichier en local, on ne le pousse pas deux fois sur GitHub.
_partage/NEBULA_pub_30s.mp4
_partage/Lighting BBQ  No Copyright Video and Sound 4K (1).mp3
_partage/Relaxing Jazz Music  Piano  Classic  Lounge (Short Jazz).mp3
# Bundle Claude Design de la piste Spider-Verse, ABANDONNÉE le 2026-07-21.
# Son README dit aux agents « implémente ce design » : ne pas le laisser traîner dans le dépôt.
_partage/actuellement-je-travails-sur-mon-outil-boussole-regarde-dans/

# Dossiers de déploiement dérivés (générés depuis la source)
clients/*/_deploy_netlify/
_dist/
clients/*/_dist/
00-nebula-agency/_dist/
# assemblé à chaque déploiement du site de l'agence depuis 00-nebula-agency/
_tmp_pages/
# résultat brut d'une fusion à arbitrer, régénéré à la demande
boussole/_dist_fusion/

# Fichiers temporaires / diagnostics
_tmp_build_pdf.py
boutique-ia/_diag_osm.py

# Mémoire Claude Code (jonction vers .claude/.../memory) — visible dans Obsidian en local,
# JAMAIS sur GitHub : contient du privé (GENÈSE, relations-famille). Ne pas retirer.
_memoire/claude-code/

# Espace PERSONNEL de Mongazi (GENÈSE, etc.) — JAMAIS sur GitHub (données sensibles). Ne pas retirer.
_perso/

# Pages de démo / diagnostic locales des vitrines & outils (jamais en prod)
boussole/_*.html
clients/*/_diag.html

__pycache__/
node_modules/
