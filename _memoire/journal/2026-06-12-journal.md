# Journal — 2026-06-12

## Vendora Support — 2e pilier (LIVRÉ + déployé)
Idée de Mongazi : un agent de SUPPORT client (pas que vendeur) pour tout business avec site/SaaS/vitrine (pilote Abakar / WE ACT). Spec : `_specs/vendora-support-v1.md`. Construit en vagues testées, **additif et inerte par défaut** (`agent_role='vendeur'`), zéro régression sur le Vendora vendeur.

Livré (tout déployé sur Railway, prod saine 200) :
- **Lot 1** — DB : `bia_merchants.agent_role`/`kb_text`/`kb_instructions` + tables `bia_knowledge` & `bia_support_tickets`. Migration Supabase appliquée.
- **Lot 2** — `core/support_agent.py` : moteur support grounded + escalade-si-doute (outil `escalader`), base injectée + cachée. Testé en réel (répond depuis KB, escalade hors-KB sans inventer).
- **Lot 3** — back-office `boutique.html` onglet « Mode Support » (rôle + base de connaissances + consignes + tickets).
- **Lot 4** — routage : si `agent_role=='support'`, le handler entrant route vers `support_agent` ; escalade → `create_support_ticket` + `_escalation_notifier`.
- **Lot 5** — import PDF (pypdf → `bia_knowledge`) : endpoints + UI.
- **Lot 6** — **widget site** : `/widget.js?code=` (bouton lumineux « Discuter avec nous » + panneau stylé aux couleurs marque) + `/api/support/chat` (CORS) + ligne à copier dans le back-office.
- **Lot 7** — `core/support_report.py` : rapport (récurrents + corrections suggérées + résumé visiteurs) via modèle writer.
- **#1** support à l'inscription (choix Vendeur/Support + FAQ avant paiement).
- **#2** lecture auto du site : `core/site_reader.py` (httpx + parser stdlib) → endpoint `/support/site` « Lire mon site ».
- **#3** le widget logge les visiteurs (id localStorage → `bia_messages`) → résumés dans le rapport.
- **#4** gating : base support pour tous ; premium (widget/PDF/site/rapport) = Business/Empire (`_support_premium_ok`).

Commits : 16347f4 (spec+fondation), 155f542 (routage), 6eda9ce (back-office), f6aaa18 (widget), d94e6fb (PDF+rapport), a23b747 (site+visiteurs), f4dea97 (onboarding+gating), 60cbd74 (kits regroupés).

## Dispatch cerveau
- `core/support.py` `_VENDORA_FACTS` enrichi du mode support (les agents qui parlent de Vendora le connaissent + futurs hériteront).
- `_memoire/vendora/capacites.md` + `decisions.md` mis à jour.

## Livrables (dossier `_kits/`)
2 kits de vente (Vendora, NEBULA) + 2 présentations (Plan du Million, Pitch investisseurs) — PDF + HTML animé. Liens de téléchargement GitHub raw. Doc Drive « Téléchargements ».

## Reste (phase 2)
RAG (grosse base), tracking des visiteurs passifs (page views), canaux Messenger/IG/email en support, agent qui vend ET supporte en même temps.

---

## Session 2 — Appels + super-vendeurs + audit + conformité APDP

### Politique d'appels + super-vendeurs (tous les cerveaux)
- **Appels = numéro du commerçant ; messages = l'agent** (aucun appel vocal de l'agent). Inscrit partout : `core/brain.py`, `core/support_agent.py`, `core/support.py` (`_VENDORA_FACTS`), `core/assistant.py`. Doc `_memoire/vendora/canaux.md`.
- **Super-vendeur** : ANALYSER le client (besoin/budget/envie/freins/pressé-hésitant) + POUSSER vers l'objectif (vente/commande/RDV/paiement). `core/brain.py` + `_memoire/vendora/vente.md`.

### Audit produit/marché/conformité
- `_audit/analyse-vendora-produit-2026-06.md` (complète l'audit technique) — recherches web. Manques 🔴 : numéro partagé (A), conformité données (B), opt-in/templates/compte officiel (C).

### Corrections 🔴 B + C (conformité APDP / Code du numérique Bénin) — LIVRÉ
- **Droit à l'effacement** : outil `effacer_mes_donnees` → `delete_customer_data()` (supprime perso, anonymise commandes, prévient le commerçant `notify_data_deletion`, ne re-sauvegarde pas).
- **Décision Mongazi** : on n'efface JAMAIS l'intelligence Vendora (leçons anonymisées/A-B/coûts) — gardée pour l'auto-amélioration même si le client part. `purge_old_messages` manuel (pas auto).
- **Opt-in** : outil `definir_preference_promos` + table `bia_optin` + `get/set_optin`. Migration `db/migrations/2026-06-12-conformite-apdp.sql` **appliquée** (Supabase anqdxzcbfuvzqxvgqzbj).
- **Page `/confidentialite`** : APDP, responsable/sous-traitant (annexe DPA), rétention, suppression côté client, consentement marketing.
- **Doc opérationnel** `_specs/whatsapp-conformite-scaling.md` (Business Verification, compte officiel, templates, Tech Provider + Embedded Signup).
- Mémoire : `_memoire/vendora/garde-fous.md` + auto-mémoire `feedback_donnees-apprentissage.md`.

### Sécurité — RLS activé (fait)
- **RLS activé** sur les 31 tables `bia_` + `bia_optin` (migration `db/migrations/2026-06-12-enable-rls.sql`, appliquée Supabase). Vendora = `service_role` (bypass RLS) → prod 200, aucune régression. Commit `75121f7`.
- Reste : 5 tables NOVA (`nova_mission`, `nova_documents`, `tool_calls`, `tasks`, `chat_history`) en RLS off → produit en pause, à traiter au redémarrage NOVA.

### §2 manque 1 — Paiement en chat (semi-auto MoMo) — LIVRÉ
- Rapprochement **montant annoncé vs total** (✅ OK / ⚠️ insuffisant / ⚠️ supérieur) + **anti-réutilisation de référence** (`payment_ref_already_used`). Notif commerçant enrichie + affichage onglet Validation. Migration `payment_amount` (`db/migrations/2026-06-12-paiement-chat.sql`, appliquée).
- ⚠️ Honnêteté : confirmation 100 % auto impossible sans agrégateur (MoMo perso) → on fait du **semi-auto** (validation 1 tap, plus rapide + plus sûr).

### §2 manque 3 — Messages interactifs WhatsApp (Cloud API) — LIVRÉ
- Outil `proposer_boutons` (cerveau vendeur) → boutons (≤3) ou liste (4-10) natifs Cloud API (`whatsapp_meta.send_buttons/send_list/send_choice`). Inbound : parsing `button_reply` + `list_reply`. **Repli texte auto** sur Twilio/Messenger/page de test (zéro régression). Catalogue produit Meta = plus tard (besoin d'un catalogue lié).

### §2 manque 4 — Analytics revenu/funnel — LIVRÉ
- `db.client.revenue_analytics()` : CA mois/semaine/jour + nb commandes, panier moyen, taux de conversion (clients qui écrivent → commandes), clients fidèles, top produits par CA. Section « Mes chiffres » en tête de l'onglet Activité (`boutique.html`). Testé (agrégations correctes).

### §3-1 — Pause IA / reprise humaine — LIVRÉ
- Table `bia_ai_pause` (migration appliquée). Helpers `is_ai_paused`/`set_ai_pause`/`paused_customers`.
- Routage : conversation en pause → l'agent SE TAIT (garde le message client + prévient la patronne `notify_paused_inbound`). Garde-fous sur les 3 canaux (Cloud/Twilio/Messenger).
- Back-office (onglet Activité) : bouton **Pause IA / Réactiver** par conversation + **champ de réponse manuelle** (envoi via Cloud API `whatsapp_meta.send_text`, vu le numéro partagé) ; endpoints `/conversation/pause` et `/conversation/send`.

### §3-2/3/4 — Robustesse (reste) — LIVRÉ
- **§3-2 anti-spam** : `count_customer_messages_window` + plafond `customer_daily_msg_cap=60`/24h → au-delà, l'agent se tait (statut `rate_limited`, aucun appel modèle = protection tokens). Garde-fous sur les 3 canaux.
- **§3-3 alerting agent-down** : `_alert_agent_down` (throttlé 30 min via `bia_settings`) → si l'envoi WhatsApp Cloud échoue (token expiré/webhook KO), Mongazi est prévenu.
- **§3-4 export/portabilité** : `export_merchant_data` + endpoint `GET /export` (JSON téléchargeable : produits, commandes, clients, caisse, ardoise) + bouton « Exporter mes données » (onglet Activité). Droit d'accès APDP.

### Chantier §2 + §3 + RLS : TERMINÉ ✅ (tout déployé)
Commits : 75121f7 (RLS), 53631d2 (paiement), 39c8483 (interactif), fe45d73 (analytics), b169038 (pause IA), b92794b (robustesse).

### Abonnements NEBULA (vitrines/catalogues) — rappels d'échéance — LIVRÉ
- **Tarifs** : vitrine **15 000 F / 3 mois**, catalogue digital **5 000 F / 3 mois** (trimestriel).
- `core/nebula_subs.py` + table `nebula_abonnements` (migration appliquée) : suivi des échéances + **rappels auto J-7 puis jour J**, idempotents (drapeaux `notif_pre_sent`/`notif_due_sent`, réarmés au paiement). Branché dans la boucle de fond (tick 30 min).
- **Canaux** : Telegram (Mongazi) + Email (Mongazi + client via Resend) = fiables ; **WhatsApp client = best-effort** (nécessite un **template Meta approuvé** hors fenêtre 24h, sinon repli silencieux sur l'email).
- **Cockpit admin** : section « Abonnements site » (ajouter client + échéance, bouton « Payé (+3 mois) », « Arrêter ») + endpoints `/api/admin/nebula-subs[/{id}/paid|/cancel]`.
- ▶️ Pour activer le rappel WhatsApp au client : faire approuver un **template** Meta (utility) — voir `_specs/whatsapp-conformite-scaling.md`.
