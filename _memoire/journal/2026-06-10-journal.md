# Journal — 2026-06-10

## Contexte
Grosse session : Page Facebook + Instagram Vendora, cadrage honnêteté des skills, modèles Sonnet, **cerveau Vendora**, back-office client WhatsApp-first + super-guide, cockpit modèles/effort, multi-compte paiement. Tout testé puis déployé.

## 1. Présence sociale (côté Mongazi)
- Page Facebook **Vendora** créée (nom, catégories, bio, infos contact, « toujours ouvert »).
- Instagram pro **@vendora_bj** créé (compte Entreprise). Lien IG↔Page en cours (propagation côté Meta).
- Visuels générés (profil + couverture) dans `_partage/vendora-fb/` (charte V dégradé violet→cyan).
- Décision vérifiée : réponse auto au PUBLIC sur Messenger/IG = **App Review Meta** requis. WhatsApp reste le seul canal autonome réel. FB/IG = aimant/contenu → WhatsApp.

## 2. Modèles IA (config + Railway)
- Règle : **Sonnet** pour le texte client (réponses vendeur + rédaction), **Opus** réservé au lourd créatif.
- `config.py` : `claude_model` → Sonnet. Variable Railway `CLAUDE_MODEL=claude-sonnet-4-6` (staged, appliquée au déploiement).
- Mémoire `feedback_modeles-vendora`.

## 3. 🧠 Cerveau Vendora (NOUVEAU)
- `_memoire/vendora/` : 11 notes interliées + index (`cerveau.md` → modeles, capacites, canaux, vente, apprentissage, prospection, tarifs, garde-fous, decisions, evolution). Dans le dossier NEBULA (Obsidian + GitHub). Règle de dispatch incluse.

## 4. Skills honnêtes (`core/capabilities.py`)
- Ajout d'un flag `soon` (badge « Bientôt », non activable, exclu de l'auto-assignation + du gating).
- Passés en « Bientôt » : **Messenger+Instagram**, **comment-to-DM**, **email pro**, **prospection**.
- Prospection : vérifié en live que OSM n'a quasi aucun email en AO (Cotonou beauté 0 / mode 1 / resto 4-60) → promesse intenable pour l'instant.

## 5. Back-office CLIENT — WhatsApp-first + super-guide (`boutique.html`, `server.py`)
- Bloc « Votre agent WhatsApp » en tête (lien + **numéro remonté** + stats).
- Onglets réordonnés (WhatsApp · Agent · Activité · Produits · Capacités · … · Réseaux en fin). Réordonnancement via flexbox `order`.
- **Super-guide** : barre de progression + étape « À FAIRE MAINTENANT » en surbrillance, le reste estompé.
- Section prospection verrouillée « Bientôt » (cohérence).

## 6. Cockpit admin — Modèles & effort (`core/model_config.py`, `admin.html`, `server.py`)
- Nouveau module `model_config` : résout modèle + effort (éco/standard/max → multiplicateur tokens) par tâche, lu depuis `bia_settings` (`cfg_models`) avec repli config + cache TTL.
- Câblé partout : brain (vendeur), manager, catalog_import, social, coach, learning, experiment, prospecting (writer), strategist (builder).
- Panneau cockpit « Modèles & effort » (par tâche : modèle + niveau), endpoint `POST /api/admin/models`. Boutons ON/OFF intacts.

## 7. Multi-compte paiement (`db/client.py`, `brain.py`, `boutique.html`, `server.py`)
- Liste de comptes Mobile Money (réseau + numéro + nom) en plus du principal, stockée en `bia_settings` (`pay_accounts_{id}`) → pas de migration.
- L'agent demande le réseau du client et donne le bon compte. UI back-office (Livraison) + endpoint `POST /api/merchants/{id}/payment-accounts`.

## Vérifs
14 fichiers Python compilent · résolveur modèles OK · 2 templates Jinja valides · aperçus rendus.

## Reste / backlog
- Lien IG↔Page (propagation) puis token Messenger + webhook + **App Review** pour réponse auto public.
- Prospection réelle (sourcing site web / segments avec emails, ou mode « liste à contacter »).
- Priorité n°1 : **premiers clients payants en direct**.

---

## 8. ASSISTANT PERSONNEL DU/DE LA PROPRIÉTAIRE — Phase A (déployé + testé en vrai)
Idée Mongazi : Vendora devient aussi le **copilote IA perso** de la patronne (sur WhatsApp ET back-office) → créer une dépendance utile (remplacer le réflexe ChatGPT).

**Nouveau cerveau `core/assistant.py`** (3e ≠ vendeur `brain.py` ≠ manager `manager.py`). 3 verrous :
1. **Identité** — `is_owner(merchant, customer)` compare les 8 derniers chiffres au `owner_whatsapp` (robuste +229/0…/espaces, multi-numéros). Branché dans `server._agent_handle` (après identif. boutique + suspension + STOP, avant `save_message`/vendeur) → la patronne bascule en mode assistant, ses messages **non persistés** (zéro pollution analytics/apprentissage). Une cliente reste en mode vendeur.
2. **Chiffres réels** — 4 outils lecture seule GROUNDED : `rapport_ventes` (aujourd_hui/hier/semaine/mois/tout), `liste_commandes`, `info_client` (par nom OU numéro), `rendez_vous`. Interrogent la vraie base ; le modèle met en forme, n'invente jamais. Fenêtres de jour en WAT (UTC+1).
3. **Honnêteté + heure réelle** — pour le hors-boutique (culture générale, « où acheter », rédaction, conseils) : réponses d'IA brillante MAIS consigne d'honnêteté (pas d'adresse/prix inventés), date+heure Bénin injectées dans le prompt.

**Back-office** : endpoint `POST /api/merchants/{id}/assistant` (session, **sans quota**) + section « Demandez à votre assistant » (`boutique.html`, fil multi-tours navigateur). **WhatsApp owner** = single-turn (Phase A). Modèle = tâche `manager` (Sonnet).

**Tests** : py_compile OK · 8 tests fonctionnels (identité/périodes/grounding) PASS · vérif sur la **vraie base prod** (boutique-test + données démo) = chiffres exacts. **Déployé** (`railway up --service boutique-ia`), route live (401). **Testé en vrai par Mongazi = très concluant.**

Boutique-test `8a0c656a-…` : `owner_whatsapp` mis au numéro de Mongazi + données de démo (3 produits, 3 commandes, 1 conversation Awa, 1 RDV) — à nettoyer sur demande.

**Phase B (next, non commencée)** : agenda/rappels en ÉCRITURE (nouvelle table) + mémoire de fil WhatsApp + éventuel web search « où acheter ». Limites Phase A : owner en vocal bloqué si forfait sans capacité `vocal` ; pas de proactif (Phase C = briefing auto, attention règle template Meta hors 24h).
