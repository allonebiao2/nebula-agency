# Journal — 2026-06-09

## Contexte
Session « on continue Vendora, étape par étape ». Grosse session : passage en PRODUCTION RÉELLE WhatsApp + plusieurs fonctions + cadrage acquisition/tarifs.

## 1. Token Meta PERMANENT (FAIT)
- Créé un **Utilisateur système** « Vendora API » (Admin) dans le Business Manager, actifs affectés en contrôle total (app « Vendora agent » + WABA).
- Token généré **expiration JAMAIS**, scopes `whatsapp_business_messaging` + `whatsapp_business_management`.
- Posé sur Railway `WHATSAPP_TOKEN` (remplace le token temporaire 24h). Vérifié via `debug_token` : `expires_at:0`, `type:SYSTEM_USER`.

## 2. VRAI numéro WhatsApp Business — LIVE & TESTÉ (FAIT)
- Modèle **numéro PARTAGÉ** (1 numéro Vendora pour toutes les boutiques, routing par code `vendora:CODE` ou par session).
- SIM neuve `0162046155`. WhatsApp Bénin = format SANS « 01 » (migration 30/11/2024) → numéro WA = **+229 62046155**.
- Nouveau WABA « NEBULA Agency » `1001537435580027`, phone_number_id `1119274114610399`.
- Fait via **Graph API + token permanent** : `register` (pin `739280`) → `{"success":true}` → statut CONNECTED ; `subscribed_apps` ✅.
- Railway MAJ : `WHATSAPP_PHONE_NUMBER_ID=1119274114610399`, `VENDORA_WHATSAPP_NUMBER=+22962046155`.
- **Testé en vrai** : Mongazi + ami écrivent au numéro → l'agent reçoit (`POST /api/whatsapp/meta 200`) et répond. Cœur = répondre aux entrants = GRATUIT illimité (1000 conv service/mois), pas de carte.

## 3. Filet de routage + liens courts (FAIT, déployé)
- `db/client.py find_merchant_by_name` + `server._agent_handle` : si ni code ni session → reconnaît la boutique par son nom, sinon demande lequel.
- `server._wa_short_link` + route `GET /go/{code}` → redirige vers le vrai lien wa.me (fini le lien à rallonge). Templates back-office/essai affichent le lien court.

## 4. Notes vocales = SOCLE + micro simulateur (FAIT, déployé)
- `core/capabilities.py` : « vocal » passé de module → **socle** (tous forfaits, gratuit Groq, hors limite).
- `server` : endpoint `POST /api/transcribe` (Groq Whisper, gratuit, tronqué 500 car., taille max 6 Mo).
- `web/templates/essai.html` : bouton **micro** (MediaRecorder, max 30s) → transcrit → envoyé comme message (compte dans la limite d'essai).
- `onboarding.html` : vocal = inclus pour tous.

## 5. Cockpit admin = centre de commande (FAIT, déployé)
- `web/templates/admin.html` : barre **« Actions rapides »** sous les stats (paiements à valider, prospection auto, recrutement manuel, mes boutiques, relances) + onglet nav « Actions ».

## 6. Divers
- Boutique-test de Roro (« Gino Fashion », code `k8aunf`) **supprimée** (SQL, toutes tables) après test.
- Tunnel souscription **vérifié de bout en bout** : MoMo `0196740732` (BIAO Mongazi Yan Karl) + alerte Telegram OK. Tout le funnel marche.
- Cadrage **tarifs** : décision = garder les prix, ré-aligner les promesses sur le livrable + offre fondateur (vocal appliqué ; reste « bientôt » Messenger/IG + Empire à appliquer).
- Cadrage **acquisition** : priorité = direct/founder-led (kit en mémoire). Email prospection = canal de scale, conforme (Resend + opt-out), JAMAIS de cold-DM WhatsApp (ban).

## Fichiers touchés (boutique-ia)
- `core/capabilities.py`, `db/client.py`, `web/server.py`
- `web/templates/essai.html`, `web/templates/onboarding.html`, `web/templates/admin.html`

## Reste / backlog
- Appliquer le reste du ré-alignement tarifs (« bientôt » Messenger/IG, Empire).
- Back-office CLIENT premium (`boutique.html`) — demandé, pas commencé.
- Moniteur fondateur (alerte crédits Anthropic épuisés).
- Setup Gmail (libellé + filtre + catch-all) pour l'email prospection à l'échelle + DMARC + sous-domaine d'envoi dédié.
- LA priorité : décrocher 3-5 boutiques payantes (direct).
