# Journal · 2026-09-03

## Ce qui a été fait

**PISTE encaisse en ligne et livre tout seul.** Parti de « vérifie, j'ai mis la
clé SasPay dans le dossier secret » ; le fichier était là, mais tout le code du
paiement dormait sur une branche `claude/…` jamais fusionnée.

1. **Fusion** de `claude/saspay-payment-integration-fv2rdt` dans `main`
2. **L'API SasPay trouvée** : `docs.saspay.me` répond depuis le PC (403 depuis
   le nuage), la sonde a ouvert une vraie session, HTTP 201
3. **Base installée** (`paiement.sql`) et **deux défauts corrigés** : la table
   est `piste.commandes` au pluriel, et la base **refusait l'état que le webhook
   allait écrire** (`payee` contre `paye`)
4. **Fonctions déployées**, secrets posés, **webhook déclaré et abonné**
5. **Site ouvert au paiement** (`SASPAY_PRET = true`), page de retour `#/merci`
   créée, formulaire adapté au moyen choisi
6. **Livraison automatique** : `piste-paiement-recu` appelle `piste-livrer`
7. **Trois sources rapatriées** (`piste-livrer`, `piste-commande`,
   `piste-signalement`) qui n'existaient que déployées

## Fichiers touchés

`piste/PAIEMENT.md` · `piste/_qc.js` · `piste/_qc_paiement.mjs` ·
`piste/_saspay_sonde.mjs` · `piste/src/donnees.js` · `piste/src/App.jsx` ·
`piste/src/composants/{Merci,Paiement,Questionnaire,Cockpit,Generateur}.jsx` ·
`piste/supabase/paiement.sql` · `piste/supabase/functions/**` · `.env.example` ·
`CLAUDE.md` · `_memoire/lecons.md` ·
`_memoire/conversations/2026-09-03-{saspay-api-trouvee,piste-paiement-en-ligne}.md`

## Contrôles

- `piste/_qc.js` : **134 verts, 0 rouge** (91 au départ)
- `piste/_qc_paiement.mjs` : **96 verts, 0 rouge** (64 au départ)

## ⏳ Reste

- **Le premier paiement réel de 200 F**, seul essai qui prouve le maillon
  reliant une notification à une commande
- **Supprimer totalement l'ancien chemin** (dépôt à la main + WhatsApp),
  demandé par Mongazi — recommandation : après le test
