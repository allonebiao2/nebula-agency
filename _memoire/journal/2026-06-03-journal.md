# Journal — 2026-06-03

## Contexte
Suite Vendora. Étage 5 (prospection) livré la veille mais envoi réel bloqué : Railway bloque le SMTP → bascule sur **Resend (API HTTP)**. Vérification du domaine `nebula-agency.online` en cours (DNS Hostinger ajoutés : DKIM + SPF MX `send` + SPF TXT `send`, propagation jusqu'à 4h). Compte d'envoi = `allonebiao2@gmail.com` puis Resend (clé valide récupérée depuis NOVA).

## Documents Drive
- **Business plan Vendora** (Google Doc dans Drive).
- **Audit complet & feuille de route** (Google Doc) : ce qui marche, capacités, gaps, étapes 30/60/90j, améliorations stratégiques. 4 piliers prioritaires identifiés : 1) facturation auto, 2) WhatsApp prod, 3) sécurité d'accès, 4) réponses entrantes.

## Pilier 3 — Sécurité d'accès au back-office (FAIT)
- Code d'accès (PIN 4–8 chiffres, style Mobile Money) + **session cookie signée HMAC** 30 jours. Colonne `bia_merchants.access_pin` (hash PBKDF2).
- Garde sur la **page** `/boutique/{id}` (écran `boutique_gate.html` : créer/entrer le code) ET sur **toutes les API de mutation** (update, products CRUD, order, prospection) via `_need_session` → 401 sinon. Le front recharge vers l'écran de code sur 401.
- Endpoint `POST /api/merchants/{id}/auth` (mode set/login) pose le cookie. Réinitialisation par l'admin (`POST /api/admin/merchants/{id}/reset-pin` + bouton « Code »).
- Testé : mauvais code 401, bon code → session, actions 401 sans / 200 avec, page protégée.

## Pilier 4 — Réponses entrantes (FAIT)
- **Opt-out / désinscription** : table `bia_optouts`. Lien de **désinscription signé** unique par destinataire ajouté à chaque email de prospection (`/unsub/{token}`). **STOP sur WhatsApp** géré dans le webhook → opt-out + confirmation. La prospection **skip** les contacts opt-out.
- **Webhook Resend** `POST /api/webhooks/resend` : `email.bounced` / `email.complained` → `blacklist_prospect_email` (opt-out + statut blacklisted) pour protéger la délivrabilité. ⚠️ À brancher dans le dashboard Resend (Webhooks → URL → events bounce/complaint).
- **Leads chauds / escalade** : nouvel outil `alerter_le_patron` dans `core/brain.py` (tool-use). Quand le client veut négocier / réclame / demande le responsable → `notify.notify_hot_lead` (Telegram à Mongazi + WhatsApp patron best-effort). Branché sur le webhook WhatsApp + `/api/chat` (hors preview).
- Testé : token unsub round-trip, `/unsub` 200 + opt-out, mauvais token 400, webhook bounce → blacklist, escalade (réclamation) → alerte.

## Fichiers touchés
- `config.py` (session_secret, public_base_url), `db/client.py` (set_merchant_pin, add_optout, is_opted_out, blacklist_prospect_email), `db/schema.sql`
- `core/brain.py` (ESCALATE_TOOL + on_escalate), `core/prospecting.py` (unsub token + footer + skip opt-out), `notify.py` (notify_hot_lead)
- `web/server.py` (auth + gate + protection API + /unsub + webhook resend + STOP + escalade + reset-pin)
- `web/templates/boutique_gate.html` (nouveau), `web/templates/boutique.html` (api 401→reload), `web/templates/admin.html` (bouton reset code)

## Migrations Supabase appliquées
- `bia_merchants.access_pin`, table `bia_optouts`.

## Conversion : Paiement à la livraison + Marchandage encadré (FAIT)
- Choix Mongazi pour la facturation : **MoMo direct sur son numéro + ID de transaction, zéro commission** (pas FedaPay pour l'instant). Pilier 1 « cycle d'abonnement » reporté.
- **Paiement à la livraison (COD)** : `bia_merchants.cod_enabled`. Quand activé, l'agent propose « payer à la réception » et confirme sans preuve d'avance. `bia_orders.payment_method` ('mobile_money'|'livraison'). Alerte commande affiche le mode de paiement.
- **Marchandage encadré** : `bia_merchants.negotiation_enabled` + `negotiation_rule`. L'agent négocie LUI-MÊME dans la limite (ex: -10%), jamais en dessous, n'escalade pas pour ça. Prix négocié enregistré dans la commande.
- Cerveau : `build_system_prompt` (sections Paiement+COD et Prix & négociation), ORDER_TOOL gagne `paiement`. Agent de gestion (`manager`) peut activer ces options par ordre. Back-office : interrupteurs (toggles) Livraison→COD et Style→Négociation + champ limite.
- Testé : négociation à -10% pile (6300 sur 7000), COD enregistré (total 7300 = 6300+1000 livraison, payment_method=livraison), pas d'escalade abusive.
- Migration : `bia_merchants.cod_enabled/negotiation_enabled/negotiation_rule`, `bia_orders.payment_method`.

## Recrutement AUTONOME + Cockpit admin premium (FAIT)
- **Recrutement auto** : Vendora se recrute des clients tout seul. Boucle asyncio in-process (démarre toujours, vérifie /30min, garde « déjà fait aujourd'hui »), cibles tournantes (catégorie×ville), volume d'échauffement. ⚠️ Appels Supabase = bloquants → TOUJOURS via `asyncio.to_thread` dans la boucle (sinon gèle l'event loop, bug rencontré et corrigé).
- **Réglages dynamiques en base** : table `bia_settings` (clé/valeur) + helpers `get_setting(_bool/_int)`/`set_setting`. Le pilote se pilote EN DIRECT depuis l'admin (ON/OFF + volume), sans redéploiement. Endpoint `POST /api/admin/recruit-settings`. Lancement manuel : `POST /api/admin/prospection/auto`. Résumé Telegram après chaque cycle. **OFF par défaut** (sécurité).
- **Modèle rédaction = Sonnet 4.6** (`writer_model`) au lieu d'Opus pour la prospection (qualité égale, ~5x moins cher). Opus réservé aux créations lourdes.
- **Cockpit admin premium** (`admin.html` réécrit) : noir profond, fond mesh animé + grille cockpit, **police futuriste Orbitron + Rajdhani**, compteurs animés (count-up), reveals au scroll, **panneau Pilote** (orbe pulsante quand ON, switch animé, volume, « lancer un cycle », total recruté). Sections : stats, pilote, paiements à valider, boutiques, recrutement manuel.
- Testé : rendu 200 (1,9s), toggle ON/OFF + volume persistés en base, 401 sans token, modèles rédaction Sonnet OK.
- ⚠️ Démarrage local de uvicorn : NE PAS chaîner `pkill ...; uvicorn` (les pkill se tuent entre eux) → lancer proprement. Premier appel DB = ~4,5s à froid (normal).

## Reste / à brancher
- Vérification domaine Resend (propagation) → puis 1re vraie campagne.
- Ajouter l'URL du webhook Resend dans le dashboard Resend.
- Variables Railway : `SESSION_SECRET`, `PUBLIC_BASE_URL` (sinon valeurs par défaut OK).
- Piliers restants : 1 (facturation auto MoMo) et 2 (WhatsApp production).

## Landing Vendora — pro + futuriste + marketing (2026-06-03)
- onboarding.html : police Plus Jakarta Sans (fini Inter), **mode jour/nuit** (theme.js + bouton nav, hero cosmique reste sombre), **tarifs alignés sur les VRAIES fonctions** (ordres/jour, prospection emails/jour, COD, marchandage ; fini les limites inventées 25 produits/300 conv), **FAQ marketing** (5 objections levées). Hero WebGL préservé. Côté démo expliqué : /essai (6 msg gratuits, preview, 0 commande) vs /demo (simulateur illimité, commandes réelles).

## Pilier 1 — Facturation automatique (modèle MoMo direct) (2026-06-03)
- Cycle d'abonnement par-dessus la validation manuelle (zéro commission). Migration : bia_merchants.period_end / last_payment_at / reminder_sent_for.
- `activate_merchant` prolonge de 30j (renouvellement = +30j depuis l'échéance si future). Helpers `subscription_active`, `days_left`, `_parse_dt`, `mark_reminder_sent`.
- `run_billing_cycle` (dans la boucle de fond, à chaque tick) : suspend auto les expirés + relance ceux à <=3j (anti-doublon via reminder_sent_for). Alertes `notify_subscription_expired/_reminder` (Telegram + WhatsApp patron).
- Garde-fou WhatsApp : suspendu OU expiré → l'agent ne vend plus. Cockpit : badge jours restants/expiré par boutique. Config subscription_days=30, reminder_days=3.
- Testé : activation +30j, expiration forcée → suspension auto, réactivation.

## Boosters + auto (2026-06-03, suite)
- VOCAL : core/transcribe.py (Twilio media -> Groq Whisper whisper-large-v3-turbo). GROQ_API_KEY sur Railway. Webhook : audio -> transcription -> agent. Testé (FR precis). NB Groq(gsk_) != Grok/xAI(xai-).
- Pilote auto COMMERCANTS : bia_merchants.auto_prospect_enabled/_category/_city + run_merchant_auto_prospection() dans la boucle (1x/jour, garde anti-doublon, abo actif + quota). Toggle back-office section Prospection.
- PHOTOS : Supabase Storage bucket public bia-products. Upload endpoint /products/{id}/photo (auth). Back-office : vignette + upload. Cerveau : outil montrer_produit + [photo] catalogue ; webhook joint <Media> TwiML. Testé (upload 200 + URL publique + agent declenche envoi).
- Pilier 1 FACTURATION AUTO deja fait (cycle abo 30j, suspension/relance auto, modele MoMo direct zero commission).
- VISION (Mongazi) : SaaS full autonome qui vit et s auto-ameliore.
- PROCHAIN confirme : AUTO-AMELIORATION (cerveau d apprentissage hebdo : ventes conclues vs perdues -> lecons -> reinjectees dans les agents + resume hebdo commercant).

## CERVEAU D'APPRENTISSAGE — auto-amélioration / intelligence collective (FAIT, 2026-06-03)
- **Objectif** : Vendora apprend de toutes les conversations → leçons de vente réinjectées dans les agents. Plus il y a de boutiques, meilleurs deviennent les agents (= le moat).
- **Moteur** `core/learning.py` : `run_learning_cycle(days=14)` relit `bia_messages`, croise `bia_orders`, reconstitue les conversations engagées (≥2 msgs client), les classe **conclues vs perdues**, échantillonne (12+12 global), construit un digest et fait analyser par **Sonnet 4.6** (`writer_model`) → leçons en puces actionnables. Leçons **globales** (collectif) + **par boutique** (si ≥8 convos, max 8 boutiques/cycle). Garde : skip si <3 conversations. `merchant_week_stats(7)` pour les bilans hebdo.
- **Stockage** : nouvelle table `bia_lessons` (scope global|merchant, lessons, stats jsonb, model). Migration appliquée sur Supabase.
- **Réinjection** : `db.get_active_lessons(merchant_id)` (global + boutique, **cache mémoire 600s** vidé à chaque save). `brain.build_system_prompt(..., lessons=)` ajoute une section « Leçons de vente apprises » ; `brain.reply(..., lessons=)`. Branché sur le **webhook WhatsApp** + `/api/chat`.
- **Autonomie** : boucle de fond → `_learning_due()` (1×/semaine, garde `bia_settings.learning_last_run`, toggle `learning_enabled` ON par défaut) → `run_learning_job(send_digests)`. Job = cycle + `notify_learning_summary` (Telegram Mongazi) + `notify_weekly_digest` par commerçant actif (anti-résiliation). Lancement manuel `POST /api/admin/learning/run` (send_digests OFF par défaut), réglage `POST /api/admin/learning/settings`.
- **Cockpit** `admin.html` : panneau « Cerveau d'apprentissage » (état, toggle, bouton Lancer l'analyse, leçons actuelles affichées) + icône `i-brain` + style `.lessons`.
- **Testé EN VRAI** : 15 conversations (6 conclues / 9 perdues / 43 600 F) → leçons globales + boutique écrites dans `bia_lessons`, injection dans le prompt confirmée, app FastAPI importée OK (routes présentes).
- **Fichiers** : `core/learning.py` (nouveau), `core/brain.py`, `db/client.py`, `db/schema.sql`, `notify.py`, `web/server.py`, `web/templates/admin.html`.
- **Déployé Railway + poussé GitHub** après validation Mongazi.

## CERVEAU — déclencheur AUTONOME (Mongazi : « il doit le faire lui-même quand c'est important et quand c'est le moment »)
- Fini le minuteur rigide 7j. `learning.should_learn(last_run)` décide seul (+ raison lisible) en combinant : 1) **volume** (≥40 nouveaux messages clients OU ≥5 ventes depuis la dernière analyse), 2) **urgence/importance** (sur 4j : ≥6 conversations engagées et <25 % de ventes = chute de conversion → apprentissage prioritaire), 3) **garde-fous cadence** (mini 18h, maxi 14j → relance dès qu'il y a du nouveau). Bouton manuel conservé. Les 4 validés par Mongazi.
- Décision peu coûteuse (pas d'appel modèle) : helpers `count_messages_since`/`count_orders_since` (count head). Le modèle ne tourne QUE si ça déclenche.
- `_learning_decision()` (serveur) → `run_learning_job(send_digests, trigger_reason)` : enregistre `bia_settings.learning_last_run` + `learning_last_trigger`, met la raison dans le résumé Telegram (`notify_learning_summary` → ligne « ⚡ Déclenché : … ») et l'affiche au cockpit (sous-titre + état « APPRENTISSAGE AUTONOME »). Manuel = raison « Lancement manuel depuis le cockpit » (send_digests OFF).
- Testé : None→première analyse ; 2h→attend (cooldown) ; 20h+peu de nouveau+conversion saine→attend ; 20j→analyse périodique. Déployé + poussé.

## GOUVERNANCE AUTONOMIE (Mongazi, 2026-06-03)
- L'agent **propose des idées / ce qu'il faut faire**, Mongazi **valide oui/non**. Il **demande avant tout sortant** (envoi, campagne, investissement). **Tout le FINANCIER (paiements, prix, investissement) reste au niveau de Mongazi.** Une capacité validée (toggle ON, OFF par défaut) tourne en autonomie dans ses garde-fous.
- Roadmap autonomie présentée (7 niveaux : acquisition auto-ciblée, onboarding/relance prospects, vente=relances, facturation/win-back, auto-amélioration, santé système, **cerveau CEO**). Priorité validée : **#1 = relances autonomes**.
- Vision élargie (à venir) : l'agent pourra **proposer d'augmenter/baisser les prix** (SaaS et/ou produits) et **proposer d'augmenter sa propre intelligence (modèle)** ; viser « super intelligent » + **capacité type machine learning** (ou skill proche). → relève du cerveau CEO + auto-expérimentation des prompts.

## RELANCES AUTONOMES — #1 (FAIT, 2026-06-03)
- L'agent recontacte de lui-même : **clients silencieux** (devis sans réponse, l'agent a parlé en dernier) + **paniers abandonnés** (`bia_orders` pending). Message rédigé par le vendeur (Haiku) avec fiche + **leçons apprises**, ton juste, légère urgence honnête.
- `core/followup.py` : `run_followups()` (gating `followups_enabled` OFF par défaut, plafonds global 200/jour + 40/boutique, cooldown 3j, opt-out respecté, boutiques actives, **fenêtre 6-22h** pour rester dans la fenêtre WhatsApp 24h → messages libres OK ; sandbox = numéros ayant rejoint). `find` réutilise `learning._build_conversations`. Vrais clients WhatsApp seulement (`customer` commence par `whatsapp:`).
- `core/brain.py` : `followup_message(merchant, products, history, lessons, kind, hours)` (1 message, sans outil). `notify.py` : `send_customer_whatsapp` (Twilio sortant) + `notify_followups_summary` (Telegram si >0).
- DB : table **`bia_followups`** (migration appliquée) + helpers `record_followup`/`followed_up_recently`/`count_followups_since`/`count_followups_today`/`list_pending_orders`.
- Serveur : `run_followups` dans la boucle de fond (chaque tick 30min). Endpoints `/api/admin/followups/settings` (toggle) + `/run` (manuel). Panneau cockpit **« Relances automatiques »** (toggle, lancer, stats jour/total, alerte si Twilio absent).
- Testé : OFF par défaut → **no-op confirmé (aucun envoi)** ; détection sans effet de bord OK. Déployé + poussé.
- PROCHAIN : #2 relance prospects non payés / acquisition auto-ciblée, ou **cerveau CEO** (pricing + auto-upgrade modèle). Puis Pilier 2 WhatsApp prod (Meta Cloud API, indispensable pour relances hors fenêtre 24h via templates).

## CERVEAU CEO — le directeur autonome qui RAISONNE par lui-même (FAIT, 2026-06-03/04)
- Demande Mongazi : « donne-lui les capacités et skills pour qu'il le fasse lui-même avec sa propre réflexion » + plus tard : proposer d'augmenter/baisser les prix, monter en intelligence (modèle), capacité ML.
- `core/strategist.py` : `gather_snapshot()` (MRR, statuts/forfaits, conversion inscription→payant + conversations→ventes 14j, expirations ≤5j, boutiques sans vente, suspendues, candidats upsell, prospection/relances ON?, leçons, prix & quotas, modèles par tâche). `run_ceo_review()` = **agent tool-use (Opus `builder_model`)** avec de vrais SKILLS qu'il appelle de lui-même : `examiner_ventes_perdues`, `examiner_boutique(nom)`, `enregistrer_recommandations`. Il enquête → conclut → propose 2-5 recommandations (constat/reco/impact/niveau/financier).
- Gouvernance : il PROPOSE, Mongazi VALIDE ✓/✗ ; financier = `financier:true`+`validation`. Stocké table **`bia_decisions`** (migration appliquée) statut proposed/approved/rejected. `notify_ceo_review` (Telegram).
- Serveur : `_ceo_due` (hebdo, `ceo_enabled` ON par défaut car il ne fait que proposer, `ceo_last_run`) + `run_ceo_job` dans la boucle. Endpoints `/api/admin/ceo/run` + `/settings` + `/decision/{id}`. Cockpit : panneau **« Décisions du directeur »** (toggle, Lancer la revue, cartes recommandations avec Valider/Rejeter). Icônes i-compass/i-x, style `.deci`.
- **Testé EN VRAI (Opus)** : 4 recommandations pertinentes enregistrées — dont il a **détecté seul un bug** (agent répondant « je ne reçois pas votre message » en boucle), recommandé de conclure dès « je confirme », d'activer les relances, et de prospecter pour sortir du client unique. `bia_decisions` = 4 proposed. Telegram envoyé.
- ⚠️ BUG À INVESTIGUER (remonté par le CEO) : conversation où l'agent dit 6× « je ne reçois pas votre message » — vérifier pipeline réception WhatsApp (encodage/média/vocal) côté webhook.
- PROCHAIN : auto-expérimentation des prompts (A/B → garde le gagnant = « ML » réaliste) + exécution auto des décisions `niveau:auto` validées. Puis Pilier 2 WhatsApp prod.

## AUTO-EXPÉRIMENTATION DES VENTES — le « ML » de Vendora (FAIT, 2026-06-04)
- Demande Mongazi : faire les 3 points, commencer par #1 (auto-expérimentation = capacité ML).
- Principe : test A/B continu (bandit champion vs challenger). Chaque variante = un bloc de TACTIQUES de vente (`variant_text`) injecté dans le prompt du vendeur. Le champion a un texte vide (= base). L'agent mesure la conversion par variante et GARDE la gagnante.
- `core/experiment.py` : `pick_variant_text(merchant_id, customer)` (OFF par défaut, assignation STABLE par client via `bia_experiment_assignments`, répartition ~équitable, cache 300s) ; `generate_challenger(lessons)` (Sonnet `writer_model`, JSON name/hypothesis/tactics, angle distinct) ; `seed_experiments()` (champion vide + 1 challenger générée) ; `evaluate_experiments()` (reconstruit convos 21j via learning, attribue via assignments, conversion/variante, **promotion auto** si leader ≥12 attribués & écart ≥15 pts → retire les perdantes) ; `run_experiment_cycle()` (amorce+évalue+relance une nouvelle challenger si 1 seul champion ≥24 échantillons).
- DB : tables **`bia_experiments`** (variant_text, status active/retired/winner, total/won) + **`bia_experiment_assignments`** (client→variante). Migrations appliquées. Helpers create/list/set_status/update_counters/get_set_assignment/list_assignments.
- Injection : webhook WhatsApp + `/api/chat` (hors essai gratuit pour ne pas polluer la conversion) → variante ajoutée aux `lessons` passées à `brain.reply`. Hook dans `run_learning_job` (tourne avec l'apprentissage). `notify_experiment_update` (Telegram : amorce/promotion/nouvelle variante).
- Cockpit : panneau **« Expériences de vente (A/B) »** (toggle OFF défaut, lancer/évaluer, variantes avec clients/ventes/% conv., variantes retirées). Endpoints `/api/admin/experiments/run` + `/settings`.
- **Testé EN VRAI** : seed OK → champion + challenger générée « Micro-engagement escalier » (3 oui successifs avant paiement, hypothèse pertinente). OFF→no-op confirmé. `experiments_enabled` laissé à **OFF** (Mongazi active quand il veut ; 2 variantes pré-amorcées prêtes).
- RESTE des 3 points : #2 exécution auto des décisions `niveau:auto` validées · #3 corriger le bug réception WhatsApp détecté par le CEO. Puis Pilier 2 WhatsApp prod.

## #2 — EXÉCUTION AUTO DES DÉCISIONS CEO VALIDÉES (FAIT, 2026-06-04)
- Boucle fermée : le CEO peut attacher une `action` exécutable (enum SÛR : `activer_relances`/`activer_prospection`/`activer_experiences`/`regler_volume_prospection`) + `action_params` à une reco `niveau:auto`+`financier:false`. À la validation ✓ par Mongazi, `strategist.execute_decision()` l'APPLIQUE seul (via `set_setting`) → décision passe en `done` + Telegram « décision appliquée automatiquement ».
- Garde-fous testés : n'applique QUE si auto + non-financier + action connue (refuse financier, validation, aucune). Jamais rien d'irréversible/financier.
- Colonnes `bia_decisions.action` + `action_params` (migration). Tool CEO `enregistrer_recommandations` enrichi + consigne. Endpoint `/api/admin/ceo/decision/{id}` exécute à l'approbation. Cockpit : badge « ✓ = appliqué auto » + toast du message d'application.

## #3 — « BUG » RÉCEPTION WHATSAPP : diagnostic + correctif (FAIT, 2026-06-04)
- Diagnostic via SQL : PAS un bug de réception. Les messages étaient bien reçus/stockés (client `trial-test-1780411145`, contenu « message numero 1..6 » = test auto). C'est le MODÈLE qui, face à des messages vagues/sans contenu, IMPROVISAIT « je ne reçois pas votre message » (faux + casse la confiance) — exactement ce que le CEO avait flairé.
- Correctif PROMPT (`brain.build_system_prompt`) : règle « Tu REÇOIS toujours le message ; ne dis JAMAIS que tu ne reçois pas/n'as pas reçu ; si c'est vague, relance avec une question de vente utile ». Testé sur « message numero 4 » / « ? » / « slt » → l'agent relance et vend au lieu de nier la réception.
- ▶️ 3 POINTS TERMINÉS. PROCHAIN possible : acquisition auto-ciblée, win-back suspendus, Pilier 2 WhatsApp prod (Meta Cloud API).

## ÉCONOMIE DE TOKENS (Mongazi : pas de clients → modérer, tokens pour les tests seulement) (2026-06-04)
- Toutes les boucles qui dépensent du modèle en autonomie = **OFF par défaut** désormais : `learning_enabled` ET `ceo_enabled` passés de ON→**OFF** (défaut code + réglages live mis à false). `followups_enabled`/`experiments_enabled`/`auto_prospection_enabled` déjà OFF. → la boucle de fond ne fait plus que la facturation (DB, gratuit) + lectures de réglages ; ZÉRO appel Claude tant que rien n'est activé.
- Le webhook WhatsApp / `/api/chat` ne consomment que sur de vrais messages = les tests de Mongazi. Les boutons « Lancer » du cockpit restent pour tester à la demande (dépense contrôlée).

## COCKPIT : ONGLETS STICKY (2026-06-04)
- Barre d'onglets `nav.tabs` sticky (sous la topbar, `top:65px`) → accès direct à chaque zone, visible au défilement : Vue d'ensemble · Directeur · Apprentissage · Expériences · Relances · Recrutement · Paiements · Boutiques · Recrut. manuel. `id` ancres sur chaque section + `scroll-margin-top:124px`. Scrollspy (IntersectionObserver) surligne l'onglet actif. Onglets scrollables horizontalement (mobile).

## REMISE À ZÉRO DU BACK-OFFICE (Mongazi : pas de vrais clients) (2026-06-04)
- Choix « tout effacer (vrai 0 partout) ». TRUNCATE de toutes les tables de données (merchants, products, orders, messages, wa_sessions, manager_commands, campaigns, prospects, optouts, lessons, followups, decisions, experiments, assignments) + suppression des clés d'historique de réglages (learning_last_run/_trigger, ceo_last_run). Vérifié : 0 partout. Réglages restants = interrupteurs (tous false) + auto_prospection_daily=15.
- La boutique-test « Little Sun Pearls » a été supprimée → recréer une boutique via la page d'inscription pour retester (code WhatsApp/PIN régénérés).

## PILIER 2 — WhatsApp PRODUCTION (Meta Cloud API) : CODE livré, dormant (2026-06-04)
- Objectif : sortir du sandbox Twilio (limité aux numéros ayant rejoint + fenêtre 24h) pour vendre/relancer n'importe quel client via l'API officielle Meta (gratuite ~1000 conv/mois, sans CB).
- **Logique d'agent factorisée** : `web/server._agent_handle(customer, body_text, has_audio, transcribe_fn)` → utilisée par Twilio ET Meta (routing vendora:code/session, garde suspension/expiration, vocal via callback, opt-out, réponse vendeur + leçons + variante A/B, photos). Twilio webhook allégé pour l'appeler.
- `core/whatsapp_meta.py` : `configured()`, `verify_webhook` (GET challenge), `send_text`, `send_image`, `fetch_media` (id→url→octets, Bearer), `parse_incoming` (texte/audio/bouton). `core/transcribe.py` : nouveau `transcribe_bytes()` (réutilisé par Meta).
- Endpoints : **GET** `/api/whatsapp/meta` (vérification) + **POST** `/api/whatsapp/meta` (messages → BackgroundTasks, répond 200 vite pour éviter les retries Meta). Sandbox `/api/whatsapp/twilio` conservé.
- Config (vars Railway à poser) : `WHATSAPP_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET` (opt.), `WHATSAPP_GRAPH_VERSION` (v21.0). DORMANT tant que token+phone_number_id absents → sandbox Twilio reste actif.
- Testé : `configured()=False`, parsing webhook OK, vérif mauvais token rejetée, `_agent_handle` no-merchant OK. Déployé (dormant).
- ⚠️ À FAIRE étape par étape avec Mongazi : compte Meta Business + WhatsApp + numéro dédié + System User token + vars Railway + URL webhook + token de vérif + templates approuvés (messages hors fenêtre 24h). Limites V1 : relances/alertes patron sortantes passent encore par Twilio (router via Meta plus tard) ; dédup messages Meta non gérée (doublon rare possible).

## PILIER 2 — VALIDÉ EN PRODUCTION (2026-06-04, soir)
- Setup Meta réalisé avec Mongazi (étape par étape) : app « Vendora agent » (968884929087817), produit WhatsApp ajouté, numéro de test +1 555-137-3708 (phone_number_id 1033003073234090, WABA 962472349467898), token temporaire généré, webhook branché (callback /api/whatsapp/meta + verify token vendora-verify-7Qm3kZ9a + champ messages). Vars Railway posées (WHATSAPP_TOKEN/_PHONE_NUMBER_ID/_VERIFY_TOKEN).
- **Gotcha résolu** : GET challenge OK mais aucun POST entrant → WABA pas abonné à l'app Vendora agent (abonné à NEBULA Sophia/Connect). Fix : POST {WABA}/subscribed_apps avec le token → success. 
- **Testé EN VRAI** : boutique de test créée (code `test`, active), Mongazi envoie « Bonjour ! (vendora:test) » depuis son WhatsApp (+22996740732) au numéro de test → message reçu côté serveur + agent répond comme Boutique Test Vendora + **réponse reçue sur le téléphone de Mongazi**. Pilier 2 = production WhatsApp officielle fonctionnelle.
- Reste : token permanent (System User), mode Live + numéro dédié + business verification (vrais clients à l'échelle), templates approuvés (hors fenêtre 24h). Token actuel expire ~24h.
- Nouvelle idée Mongazi (à étudier) : que l'agent PROSPECTE sur Facebook/Instagram (contacter des clients potentiels selon leurs préférences). → voir analyse de faisabilité (politiques Meta : prospection non sollicitée en DM = interdite/risquée ; voies conformes = Click-to-WhatsApp Ads, lead ads, commentaires→DM opt-in, contenu organique).

## NETTOYAGE TOKENS + BACKLOG (2026-06-04, suite)
- Mongazi : économie de tokens (pas de clients) + remise à 0 du back-office (faits plus haut). Backlog priorisé : 1) forfait flexible 2) essai optionnel 3) emails clients (domaine) 4) inbound Messenger/IG. Choix : commencer par 1 + 2.

## FORFAIT FLEXIBLE (downgrade/upgrade) (FAIT, 2026-06-04)
- Colonne `bia_merchants.pending_plan` (migration). `set_merchant_plan(id, plan, immediate)` : immediate=True applique tout de suite (upgrade payé), False = programme au prochain renouvellement (downgrade, garde le forfait payé jusqu'à period_end). `activate_merchant` applique le `pending_plan` au renouvellement (puis le vide).
- Endpoint admin `POST /api/admin/merchants/{id}/plan` {plan, apply:'now'|'renewal'}. **Garde-fou** : un downgrade ne s'applique JAMAIS « maintenant » (forcé au renouvellement) même si on demande now. Financier → Mongazi décide.
- Cockpit (Boutiques) : `<select>` de forfait par ligne (changePlan : upgrade demande maintenant/renouvellement, downgrade = renouvellement) + badge « → {forfait} au renouv. » si pending. Style `.planpick`. Row enrichie (plan_key/pending_plan/pending_label).
- Testé en vrai sur boutique-test : downgrade business→demarrage programmé (plan reste business, pending=demarrage) → renouvellement applique (plan=demarrage) ; upgrade immédiat →empire. (remis business).

## ESSAI GRATUIT OPTIONNEL (FAIT, 2026-06-04)
- L'onboarding ne FORCE plus l'essai : après création, redirection vers `/activation/{id}` (souscription directe) au lieu de `/essai/{id}`. Page activation : lien « Testez-le gratuitement d'abord → (optionnel) ». Essai (`/essai`) garde déjà son bouton Activer.
- Textes adaptés (onboarding) : étapes (« Activez en 1 paiement », test « optionnel »), sous-titre tarifs (« Souscrivez directement, ou testez gratuitement d'abord »), FAQ, bandeau prix (plus de « Essai gratuit, puis »). JS planPrice/planLabel inchangé (pas de réinjection « essai »).

## INBOUND MESSENGER + INSTAGRAM DIRECT (FAIT, dormant, 2026-06-04)
- Backlog #4 : l'agent reçoit/répond aussi sur **Facebook Messenger** et **Instagram Direct** (même cerveau/catalogue/leçons/A-B que WhatsApp). S'appuie sur `_agent_handle` (déjà factorisé canal-agnostique au Pilier 2). **DORMANT** tant que `MESSENGER_PAGE_TOKEN` absent.
- `core/messenger_meta.py` (calqué sur `whatsapp_meta.py`, mais Messenger Platform ≠ WhatsApp Cloud) : `configured()`, `verify_webhook` (fallback sur `whatsapp_verify_token`), `send_text` (2000 car.), `send_image` (légende → texte avant image car Messenger ne légende pas), `fetch_media` (URL CDN directe, **1 seul GET sans token** ≠ WhatsApp 2 appels Graph), `parse_incoming`. Pièges gérés : événements sous `entry[].messaging[]` (≠ `changes[].value.messages[]`), expéditeur = PSID/IGSID (≠ numéro), **échos `is_echo` de nos envois ignorés**, accusés delivery/read/réactions ignorés, image/sticker sans texte ignorés, vocal (audio attachment) + postback (clic bouton).
- Endpoints : **GET** `/api/messenger/meta` (vérif webhook) + **POST** (messages → BackgroundTasks, 200 rapide). `customer` = `messenger:{psid}` / `instagram:{igsid}` (distinct de `whatsapp:+…`).
- **Résolution Page→boutique** : un visiteur dit juste « Bonjour » (pas de `vendora:code`). `entry.id` (ID Page/compte IG) mappé à un **code boutique** via `bia_settings` (clé `page_merchant_{page_id}`) → la session est pré-amorcée (`upsert_wa_session`) avant l'agent. Endpoint admin `POST /api/admin/messenger/link` {page_id, code} (set/clear, garde 401/400/404). Helper DB `list_settings_prefix`. Config `messenger_page_token` + `messenger_verify_token`.
- **Cockpit** : nouvel onglet + section **« Canaux entrants »** (badges Messenger/IG + WhatsApp connecté/dormant, formulaire associer Page→code avec datalist des codes, liste des associations + retirer). CSS `.chrow/.linkform/.links`, JS `linkPage/unlinkPage`.
- **Testé EN LOCAL** : parsing (texte Messenger/IG, vocal, écho ignoré, delivery ignoré, image sans texte ignorée), `configured()=False`, verify dormant→403, POST webhook→200, `/admin` rend la section, endpoint link 401/400/404 + happy path (mapping écrit/effacé en base), **chaîne complète Page→boutique→session validée sans appel modèle** (retrouve « Boutique Test Vendora » active). `.env.example` complété (Meta WhatsApp + Messenger).
- ▶️ RESTE étape par étape avec Mongazi (config Meta, pas du code) : créer une Page FB + compte IG pro lié, App « Vendora agent » → produit Messenger + Instagram, **Page Access Token** (System User, longue durée), abonner la Page aux webhooks (champ `messages`), poser `MESSENGER_PAGE_TOKEN` (+ verify) sur Railway, brancher le callback `/api/messenger/meta`, puis associer la Page à la boutique dans le cockpit. Limite V1 : envois SORTANTS (relances) restent WhatsApp ; dédup messages Meta non gérée (doublon rare).

## « COMPOSEZ VOTRE VENDEUR » — capacités à la carte par forfait + PRISE DE RDV (FAIT, 2026-06-04)
- Idée Mongazi : le commerçant choisit ce que son agent sait faire ; choix limité selon le forfait ; l'agent s'adapte. Réflexion + recadrage donnés (3 couches, gating hybride palier+nombre, bénéfices pas features, defaults par métier, pièges : ne jamais brider la vente de base / éviter friction inscription / ne pas exposer la machinerie interne). Grille validée par Mongazi + RDV en version « disponibilités configurables ». Construit en 3 vagues testées (68/68 mocks).
- **GRILLE** : 🟢 Socle (tous forfaits, vend 24/7 + commandes + conseil + alerte patron) · 🔵 Modules à la carte (Démarrage : vocal/photos/cod/négo ; Business+ : relances/rdv/multicanal/prospection) · 🟣 Super-pouvoirs Empire (apprentissage perso/email pro/comment-to-DM). Plafonds : **Démarrage 2 modules · Business 5 · Empire illimité**.
- **Vague 1 — `core/capabilities.py`** (registre = source de vérité unique) : `CAPABILITIES` (id/group/min_plan/label/promise/icon), `MODULE_LIMIT`, `effective_capabilities` (socle + choix ∩ accessibles, plafonné ; **repli legacy** sur anciens interrupteurs si colonne NULL → zéro régression), `has_capability`, `default_capabilities_for(métier, forfait)` (bundles `CATEGORY_RECO` + filler), `sanitize_selection` (grille appliquée serveur), `capabilities_context`/`selectable_for_plan` (UI). Branché dans `brain.build_system_prompt` (COD/négo/photos/RDV pilotés par caps) + `reply` (outils conditionnels). **Gating réel partout** : `followup._eligible` (relances), prospection auto (server), `_process_messenger_message` (multicanal), `_agent_handle` (vocal → invite à écrire si absent), `_process_comment` (comment_to_dm), `learning` (leçons perso = apprentissage_perso). Rétrocompatible (test boutique NULL → legacy vocal+photos+toggles).
- **Vague 2 — écran « Composez votre vendeur »** : defaults intelligents posés à la création (`create_merchant_endpoint`, jamais blanc) ; éditeur back-office (onglet *Capacités* dans `boutique.html` : cartes cliquables, compteur X/limite, cadenas = upsell + « Débloquer plus → ») ; endpoint `POST /api/merchants/{id}/capabilities` (session-protégé, `sanitize_selection` côté serveur) + `db.set_merchant_capabilities`. Colonne `bia_merchants.enabled_capabilities` (csv).
- **Vague 3 — PRISE DE RDV 🆕** (capacité `rdv`, Business+) : `brain.RDV_TOOL` (`enregistrer_rendezvous`) + bloc « # Rendez-vous » dans le prompt (dispo injectées, gated) + `reply(on_appointment=…)` ; `server._appointment_recorder` → `db.create_appointment` + `notify.notify_appointment` (Telegram + WhatsApp patron) ; back-office section *Rendez-vous* (dispo `rdv_days`/`rdv_hours`/`rdv_note` + liste des demandes), visible si capacité active. Table `bia_appointments` + colonnes `rdv_*`.
- **Migration Supabase appliquée** (additive/idempotente, projet `anqdxzcbfuvzqxvgqzbj`) : `enabled_capabilities`, `rdv_days/hours/note`, table `bia_appointments` (+ index). Back-office à 0 client → aucun risque sur l'existant.
- **Testé local (mocks, sans DB ni API)** : 68/68 — registre/limites/effective/legacy/defaults, branchement cerveau (COD/négo/photos/RDV présents/absents selon caps), `sanitize_selection`, `capabilities_context`, compilation Jinja (boutique+admin), helpers DB/notify, signature `reply`. Imports serveur OK.
- ▶️ RESTE (optionnel, non bloquant) : reco automatique du **Cerveau CEO** « active telle capacité » selon les données (OFF par défaut) ; étape de choix directement dans le tunnel d'inscription (écartée pour l'instant = friction d'activation, le choix vit au back-office + defaults au signup) ; prettifier les icônes/preview live.

## DÉPLOIEMENT + BACK-OFFICE PLUS EXPLICITE (2026-06-04)
- **Gotcha rappelé** : un `git push` ne déploie PAS Vendora — il faut `railway up --service boutique-ia --detach` (projet impartial-achievement, service boutique-ia, domaine vendora-agent). Comment-to-DM + « Composez votre vendeur » étaient sur GitHub mais pas en ligne → Mongazi ne voyait « aucune différence ». Déployés. Migration capacités/RDV appliquée (Supabase MCP).
- **Retour Mongazi** : « je ne comprends rien, le back-office n'est pas clair » → besoin d'un « voici comment utiliser » + guider le remplissage. Construit :
  - **Guide de démarrage** en haut du back-office (`boutique.html` + contexte `guide` dans la route `/boutique/{id}`) : encadré « 👋 Bienvenue » + étapes ordonnées (produits / composez votre vendeur / paiement-livraison / tester / partager le lien / activer), **état fait/à faire calculé auto** (produits présents, enabled_capabilities posé, momo_number, statut actif), compteur X/total, boutons `goTo()` qui scrollent au bon onglet, message « tout est prêt » quand complet.
  - **Explications sous chaque onglet** (Infos/Livraison/Style) — chacun se comprend seul.
  - **Choix multiples (chips) + exemples** ajoutés là où il manquait : **RDV** (jours Lun→Dim, horaires, précision), **zones de livraison** (villes Cotonou/Calavi/…), **frais** de livraison. (Produits/Infos/Style avaient déjà chips+placeholders, mais invisibles faute de déploiement.)
- Commits : `c94884f` (guide + formulaires). Validé + déployé. CLAUDE.md « jamais pusher sans validation » a bien bloqué un push auto → validation explicite demandée puis push.
- ▶️ ROADMAP en cours : reprendre **② boîte email** (étape 1 = activer IMAP Gmail, en attente) puis **③ token Meta permanent**.

## PAGE DE VENTE VENDORA — refonte (nav, futuriste, essai séparé, paiement) (2026-06-04)
- Retour Mongazi sur la plateforme d'achat : meilleure navigation, look futuriste (émojis → icônes), essai gratuit séparé des offres, paiement allégé. + clarif : **seul le forfait Démarrage (5 000 F) est testable**, et le « tester » doit être **un bouton** à côté de la création (pas un texte qui embrouille).
- `onboarding.html` : **sprite SVG ligne** (futuriste) → **tous les émojis remplacés** (avantages, offres 🌱⭐👑→fusée/étoile/couronne, en-têtes formulaire, bandeau défilant ; 0 émoji restant vérifié). **Nav** avec ancres (Avantages · Comment ça marche · Tarifs · Essai gratuit · FAQ). **Essai = Démarrage uniquement** : carte Démarrage marquée « ✓ Testable gratuitement », bandeau `#essai` reformulé (offre Démarrage), et en **fin de formulaire 2 boutons** : « Créer mon agent » + « Tester gratuitement » — ce dernier **affiché seulement si Démarrage sélectionné** (JS `refreshOffer`/`startTrial` ; `goTrial`→redirection `/essai` sinon `/activation`). Texte embrouillant du bandeau prix supprimé.
- `activation.html` : **allégée** (moins de texte) + **sélecteur des 3 offres** (`plan-pick`, montant dynamique) → le choix de l'offre se fait À L'ACTIVATION, plus collé au 5 000. Route `/activation/{id}` passe `plans`+`current_plan`. `submit_payment` accepte `plan` → `set_merchant_plan(immediate=True)` avant d'enregistrer la réf.
- `essai.html` : CTA neutralisé (« Choisir mon offre et activer » au lieu de « Activer — 5 000 F »), émojis retirés.
- **Testé en local** (uvicorn :8010, DB prod via .env) : accueil/activation/essai 200, sélecteur d'offre rendu (data-plan ×3), bouton essai conditionnel, 0 émoji. Gotcha Windows : `pkill -f uvicorn` ne tue pas le process → utiliser PowerShell `Get-NetTCPConnection -LocalPort 8010 | Stop-Process` pour libérer le port avant de relancer.
- Validé par Mongazi → commit + push + déploiement Railway.

## POLISH PLATEFORME — mobile + hero + cohérence émojis (2026-06-04)
- Mongazi : « continuer à polir » (a coché les 4 : mobile, page démo, hero, écran code d'accès).
- **Mobile** (`onboarding.html`) : media query `max-width:560px` → champs `.frow` empilés, boutons `.go-row` (Créer/Tester) empilés pleine largeur, sections/hero compacts, bandeau d'essai centré, formulaire moins serré. (demo/essai/gate déjà responsives via design system.)
- **Hero** : accroche reformulée (« un vendeur qui ne dort jamais… ») + 2ᵉ bouton du hero → `#essai` (essai gratuit).
- **Cohérence futuriste** : émojis retirés de `demo.html` (👇 ⚠️), `essai.html` (⚠️), `activation.html` (✅), `boutique_gate.html` (🔒). La page de vente était déjà à 0 émoji (les ✓/✕ sont des symboles de liste voulus).
- **Clarif Mongazi** sur le flux : page de vente = 1 seule page (défile) ; à la fin du formulaire, le clic **crée la boutique + redirige** vers `/activation` (Créer) ou `/essai` (Tester, Démarrage uniquement) → puis `/boutique`. Confirmé que la phrase « Vous pourrez tester gratuitement » qu'il revoyait = **cache navigateur** (prod déjà OK depuis `d6da215`). ⚠️ Penser à dire « Ctrl+F5 / onglet privé » quand un changement déployé ne s'affiche pas.
- Validé → commit + push + déploiement Railway.

## BOÎTE EMAIL — ISOLATION (ne touche jamais la boîte perso) (2026-06-04)
- **Découverte** (test IMAP `railway run` lecture seule) : creds IMAP déjà valides sur Railway + IMAP activé, MAIS `allonebiao2@gmail.com` = boîte **PERSO** (2 674 non lus). Activer l'inbox telle quelle aurait marqué ses mails persos comme lus → risque signalé à Mongazi. Choix Mongazi : **isoler**.
- **Code** : `inbox.fetch_unseen` lit désormais **UNIQUEMENT le libellé `settings.inbox_mailbox`** (défaut « Vendora ») au lieu de `INBOX`. Si le libellé n'existe pas → **ne lit RIEN** (sécurité : boîte perso jamais touchée), ne fait même pas le `search`. Réglage `inbox_mailbox` (env `INBOX_MAILBOX`) dans config.
- **Testé (mocks)** : 8/8 — lit le libellé Vendora, **jamais INBOX**, marque \Seen ; libellé absent → `[]` + pas de search. Import serveur OK.
- ▶️ **POUR ACTIVER en vrai (setup, plus tard, quand prospection)** : (1) créer le libellé Gmail **« Vendora »** ; (2) **filtre Gmail** : réponses Vendora → appliquer libellé « Vendora » + « ignorer la boîte de réception » ; (3) pour que les réponses recrutement soient filtrables, mettre `EMAIL_REPLY_TO=allonebiao2+vendora@gmail.com` (plus-adressage) sur Railway ; (4) pour les boutiques : renvoi catch-all `*@nebula-agency.online`→gmail (Hostinger) + même filtre. Tant que rien de tout ça : inbox OFF + lecture vide = zéro risque.

## LEVIERS DE CONVERSION — page de vente (2026-06-05)
- Mongazi : « qu'est-ce qui augmente drastiquement le taux d'adhésion ? ». Recherche web (benchmarks SaaS : médiane essai→payant 18,5 %, élite 60 % ; time-to-value <10-15 min = clé ; 1 touche humaine = +6-12 pts ; essai court+urgence +71 % ; preuve sociale temps réel +98 %, vidéo +80 %). Réponse donnée + cadrée honnêtement (pas de 99 %).
- **Implémenté (gratuit, honnête, sans dépendance)** dans `onboarding.html` : **section « Rentabilité » (#roi)** = comparaison vendeur humain (30-50k F, dort, rate la nuit) vs Vendora (dès 5k, 24/7) + punchline **« 1 vente récupérée = abonnement remboursé »** ; **bande réassurance** (sans engagement · sans CB · annulez quand vous voulez · satisfait ou remboursé) ; **offre « membre fondateur »** (urgence honnête) ; nav « Rentabilité ». CSS `.roi-compare/.roi-col/.reassure/.founder` + responsive.
- **PAS de faux témoignages** (décision : honnêteté, risqué sinon) → structure preuve sociale à remplir avec de vrais avis + captures dès les 1ers clients.
- ▶️ Leviers les + puissants restants = **preuve de valeur à la coupure** (« ton agent a généré X ventes pendant l'essai ») + **message humain auto** pendant l'essai → nécessitent le **système d'essai 3 jours (SUSPENSION, jamais suppression — garder les données = arme de conversion)**, à construire (réutilise la facturation existante qui suspend déjà les expirés). Validé → déployé.

## SYSTÈME D'ESSAI 3 JOURS — suspension (jamais suppression) + preuve de valeur (FAIT, 2026-06-05)
- Idée Mongazi : essai 3 jours → couper auto + supprimer l'agent. Recadré : **couper OUI, supprimer NON → SUSPENDRE** (garder les données = arme de conversion). Validé.
- **Réutilise la facturation existante** (`run_billing_cycle` suspend déjà les expirés). Un essai = `is_trial=true` + `status=active` + `period_end=now+3j`. Colonne `bia_merchants.is_trial` (migration Supabase appliquée).
- **DB** : `start_trial(id, days=3)` (active, is_trial, period_end, activated_at) ; `activate_merchant` pose `is_trial=False` (le paiement met fin à l'essai) ; `trial_stats(id)` = {conversations, orders, revenue} (preuve de valeur).
- **Inscription** : bouton « Tester gratuitement » (offre Démarrage) envoie `trial:true` → `create_merchant_endpoint` appelle `start_trial` → redirige `/essai`.
- **Facturation essai-aware** (`run_billing_cycle`) : essai fini (J≤0) → suspend + `notify_trial_ended` (preuve de valeur) ≠ subscription_expired ; rappel **J-1** (fenêtre 1j pour essai vs 3j abo) → `notify_trial_reminder`. `notify.py` : `_trial_value_line` + 2 notifs (Telegram Mongazi + WhatsApp patron).
- **UI** : bandeau « Essai gratuit — il reste X jours » + bouton Activer sur `/essai` ET back-office ; le guide ne compte plus l'essai comme « activé » ; **MRR cockpit exclut les essais** (actifs mais non payants) + compteur `trials`.
- **V1** : preuve de valeur **affichée + Telegram** (envoi auto WhatsApp au commerçant = plus tard, infra Meta). Réactivation = 1 paiement → tout intact.
- **Testé (mocks) 15/15** : start_trial/activate champs, billing essai (suspend→trial_ended, J-1 rappel, J-2 pas de rappel, abo→subscription_*), preuve de valeur. Import + templates OK. Validé → déployé.

## ACQUISITION SOCIALE CONFORME (échange Mongazi) — comment-to-DM, PAS de cold DM
- Mongazi a demandé si l'agent peut « aller chercher » des clients sur Messenger/IG. Réponse donnée + **avertissement** (nouvelle règle : prévenir des risques avec le pourquoi, cf. [[feedback_avertir-risques]]) : **cold DM = interdit Meta + bannissement**, ET techniquement bloqué (fenêtre 24h : on ne peut écrire qu'à qui nous a écrit). Voies conformes : **comment-to-DM** (l'agent répond aux commentaires → DM opt-in), inbound (fait), Click-to-WhatsApp Ads (payant → écarté, pas de CB). Mongazi : « d'accord on y va » sur comment-to-DM (à coder après).

## BOÎTE EMAIL ENTRANTE (Gmail) — l'agent REÇOIT et RÉPOND (2026-06-04)
- Backlog email : « prospecter » (envoi) était déjà fait ; manquait **recevoir + répondre**. Construit la boucle entrante.
- **Recrutement** (réponses qui reviennent dans NOTRE boîte = reply-to `allonebiao2@gmail.com`) : `core/inbox.py` lit l'IMAP Gmail (UNSEEN, marque \\Seen), parse (from/to/subject/body, **nettoyage des citations FR/EN**, Message-ID pour dédup), **ne répond QU'AUX prospects connus** (`find_recruitment_prospect`, sécurité anti-spam), gère **STOP/désinscription** → opt-out, garde-fous (plafond 60/j, cooldown 12h, ignore no-reply/mailer-daemon/nos propres adresses). Réponse rédigée par **Sonnet** (`prospecting.compose_recruitment_reply` : vend Vendora, lien d'inscription, signée Mongazi), envoyée via **Resend**, tracée dans table **`bia_inbox`** (migration appliquée). Boucle de fond (`run_inbox`, OFF par défaut `inbox_enabled`). Endpoints `/api/admin/inbox/settings` + `/run`. Cockpit : panneau **« Boîte email — réponses »** (onglet + toggle + lancer + stats). DORMANT tant que GMAIL_USER/_APP_PASSWORD absents + Resend.
- **Boutiques** (les deux modes, toggle par boutique — choix Mongazi) : chaque boutique prospecte depuis son **alias `code@nebula-agency.online`** (From + reply-to → les réponses reviennent chez nous, routables). `inbox.run_inbox` généralisé : **route par le header To** (`_route_merchant` → `get_merchant_by_code(local)`) → flux boutique sinon recrutement. Réponse vendeur par `brain.email_reply` (produits + leçons). **Mode supervisé** (`bia_merchants.inbox_mode`='review', défaut) = l'agent rédige un **BROUILLON** (`bia_inbox.status='draft'`), le commerçant **valide/modifie/rejette** dans son back-office (section **« Réponses à vos prospects »** + endpoints `/api/merchants/{id}/inbox/mode` et `/inbox/draft/{row_id}`). **Mode auto** = envoi direct depuis l'alias. Toggle global admin `boutique_inbox_enabled` (OFF) + compteur brouillons au cockpit. `send_email` gagne `from_address` (doit être sur le domaine vérifié). Garde-fou : si la boîte boutique est OFF, la prospection garde son comportement actuel (reply-to = email du commerçant).
- **Pourquoi le From reste notre domaine** : Resend n'envoie que depuis un domaine vérifié → impossible d'usurper le @gmail du client (DMARC). On met le nom de la boutique en affichage + l'alias `code@domaine`.
- **Testé EN LOCAL (sans IMAP/Claude/envoi réel, mocks)** : parsing To + citations FR/EN, routing alias (test→boutique, contact/inconnu→None), **mode supervisé → brouillon créé, ZÉRO envoi**, **mode auto → envoi via alias**, **STOP → opt-out**, **inconnu → skip**, dormant OFF = no-op, rendus admin + back-office OK, endpoints commerçant 401 sans session. Migrations `bia_inbox` (+ merchant_id/status) et `bia_merchants.inbox_mode` appliquées. Tous les toggles laissés **OFF** (économie tokens).
- ▶️ SETUP requis (étape par étape, plus tard) : **renvoi catch-all `*@nebula-agency.online` → `allonebiao2@gmail.com`** (Hostinger) pour que l'agent lise les réponses des boutiques ; vérifier IMAP non bloqué par Railway (port 993). Idée future : transférer une copie au gmail du commerçant pour qu'il voie le fil dans sa propre boîte.

## COMMENT-TO-DM — acquisition sociale CONFORME (FAIT, dormant/OFF, 2026-06-04)
- Roadmap point #1 (validé la session passée, cf. [[feedback_avertir-risques]]). Quand quelqu'un **commente** une publication de la Page FB / compte IG d'une boutique, l'agent lui répond **en privé (DM)** pour démarrer la vente. Le commentaire = consentement → seul canal d'acquisition sociale autorisé Meta (« Private Replies »). **Cold DM jamais** (interdit + bannissement + bloqué par la fenêtre 24h). Une fois le DM ouvert, le flux Messenger/IG existant prend le relais (`_process_messenger_message`).
- `core/messenger_meta.py` (+105) : `parse_comments` (lit `entry[].changes[]` : FB field `feed` item=comment verb=add ; IG field `comments` ; ignore nos propres commentaires from==page, éditions/suppressions, réactions, posts) ; `send_private_reply(page_id, comment_id, text)` (cible `recipient.comment_id`, `POST /{page_id}/messages` + Page token, marche FB **et** IG) ; `reply_public_comment(comment_id, text, platform)` (accusé public : FB `/{cid}/comments`, IG `/{cid}/replies`).
- `web/server.py` (+101) : `_process_comment` (tâche de fond) avec **garde-fous câblés** : OFF par défaut (`c2dm_enabled`), n'agit QUE sur Pages mappées à une boutique connue (`page_merchant_{page_id}`), boutique suspendue/expirée → skip, **mot-clé déclencheur optionnel** (`c2dm_keyword`, ex « PRIX »), **opener templaté = ZÉRO appel modèle** (le cerveau ne s'allume qu'à la réponse client → économie tokens), pré-amorce la session DM (`upsert_wa_session messenger:psid / instagram:igsid`), **dédup mémoire bornée à 1000** (`_c2dm_is_new` ; backstop = Meta refuse la 2e private reply), ack public optionnel (`c2dm_public_reply`, défaut ON). Webhook `/api/messenger/meta` route aussi les commentaires. Endpoint admin `POST /api/admin/c2dm/settings` (enabled/public_reply/keyword).
- **Cockpit** (`web/templates/admin.html`, +37) : panneau **« Comment-to-DM »** dans la section *Canaux entrants* (toggle activer, champ mot-clé déclencheur, toggle réponse publique) + JS `toggleC2dm/toggleC2dmPub/saveC2dmKw`. Hint conformité (commentaire=consentement, jamais de cold DM).
- **Testé EN LOCAL (mocks httpx + db, sans réseau ni Claude)** : 28/28 OK — parsing FB/IG (commentaire valide, propre commentaire ignoré, verb≠add ignoré, réaction ignorée, message ≠ commentaire), envoi privé (URL `/page/messages` + cible comment_id + token, code 400→False, exception→False), public FB vs IG (endpoints distincts), flux complet (OFF→rien, ON→DM+ack+session, dédup, page non mappée→rien, filtre mot-clé insensible casse, suspendue→rien, expirée→rien, public_reply OFF→DM seul). Tout laissé **OFF** (économie tokens).
- Pas de nouvelle var Railway (réutilise `MESSENGER_PAGE_TOKEN`). ▶️ POUR ACTIVER EN VRAI (avec le setup Messenger/IG, plus tard) : abonner la Page aux **champs webhook `feed` (FB) + `comments` (IG)** + permissions `pages_manage_metadata` / `instagram_manage_comments`, puis toggle cockpit.
