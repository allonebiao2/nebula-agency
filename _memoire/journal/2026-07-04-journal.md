# Journal — 2026-07-04

## HH DESIGN (client 08) — refonte en expérience 3D immersive « ultra 3D »
Réécriture complète de `clients/08-hh-design/index.html` en **parcours 3D** (CSS pur,
perspective + preserve-3d, GPU, self-contained ~68 Ko) au lieu de la page défilante.

### Parcours
Extérieur (façade + porte bois + bouton « Voir ») → la porte s'ouvre en 3D + flash, la
**caméra traverse** → **Hall à 3 portes** (Collection · Sur-mesure · Atelier) → clic sur une
porte → **salle 3D dédiée**. Décision Mongazi : « remplace tout par du 3D », mais **tout le
vrai contenu est gardé** dans les salles (6 pièces + fiches, sur-mesure, finitions, formulaire).

### Passe « ultra 3D » (demande explicite)
- Portes **volumétriques** (embrasure à 4 jambages + pièce entrevue au fond ; moulure =
  anneau creux → l'ouverture révèle l'intérieur éclairé).
- Pièces **closes** : plafond + **lampe suspendue** qui se balance + cône de lumière.
- Caméra **vivante** : dérive lente continue + parallaxe pointeur (contenu + décor des salles).
- Travelling plus profond (repos −2200px, traversée +1250px scale 2.7 + flou).

### Garde-fous
- Contenu réel intact ; n° WhatsApp `2290162686768` **inchangé** (x5).
- Fallback `prefers-reduced-motion` → page plate scrollable, contenu dans le DOM (a11y + SEO).
- QC : `node --check` JS OK ; captures headless desktop + mobile + porte ouverte + reduced-motion.

### Leçons (dispatchées dans la conversation du jour)
- Ne jamais masquer le contenu par défaut en attendant une classe de reveal (page blanche
  headless) → visible par défaut, l'anim n'est qu'un enrichissement.
- Cadre de porte plein en translateZ positif masque le battant → utiliser un anneau creux.
- Playwright : la dérive caméra continue empêche la stabilité → `click({force:true})`.

## Fichiers touchés
- `clients/08-hh-design/index.html` (réécrit) — commits `c3b76bb` (3D) + `382a3cc` (ultra-3D).
- `_memoire/conversations/2026-07-04-hh-design-3d.md` (log détaillé).
- CONTEXT client 08 + CLAUDE.md (ligne client) mis à jour.

## Reste à faire
- **Déploiement Cloudflare Pages à lancer EN LOCAL** (pas de wrangler/credentials sur l'env
  distant) : `wrangler pages deploy _dist --project-name=hh-design`. Le site LIVE
  `hh-design.pages.dev` est encore sur l'ancienne version.
- ⚠️ Confirmer le n° WhatsApp `+229 01 62 68 67 68`.
- Reste hérité : vrai logo HH, Instagram, adresse Maps, prix.
