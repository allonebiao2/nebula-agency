# Journal — 2026-06-14

## Vitrina (nouveau produit) — conçu, déployé, sécurisé, en ligne
- Création de **Vitrina** : générateur automatique de vitrines pour micro-commerçants francophones (marque **autonome & anonyme**, machine de volume derrière NEBULA — stratégie 2 marques).
- Dossier `vitrina/` : `index.html` (vente), `creer.html` (générateur 3 thèmes + **gating par pack** + aperçu live + logo + photos base64 + checkout), `exemple-beaute.html`, `server.py` (FastAPI + SQLite : commande, back-office, `/v/{slug}`, Telegram), `cfproxy/_worker.js` (proxy), `requirements.txt`, `Procfile`, `presentation-vitrina.html/.pdf`. Secrets dans `.env` (gitignored).
- **Paiement MTN MoMo manuel** + notif **Telegram** (@Vitrina_nebulabot) + back-office de validation.
- **Déployé LIVE : https://vitrina.nebula-agency.online** — Railway (backend, volume `/data`) + **proxy Cloudflare Pages** (`vitrina-proxy`) + **DNS Hostinger** (CNAME). Contourne le blocage domaine custom de Railway (gratuit).
- **Back-office sécurisé** : login mot de passe + cookie HMAC (fin du `?key=`) + boutons Suppr./Déconnexion ; **commandes test nettoyées**.
- **Email obligatoire** à la commande + **échéance hébergement (+1 an)** stockée (base des rappels).
- Présentation commerciale PDF générée + hébergée (`/presentation-vitrina.pdf`).

## Cerveau Vitrina segmenté (`_memoire/vitrina/`)
- Index `cerveau-vitrina.md` + **7 segments** (01 identité/vision … 07 décisions/apprentissages) + convention de mise à jour + journal du cerveau. Vision long terme = atelier digital self-service (suite de modules).

## Exploration niche Relations / Famille (`_memoire/relations-famille/`)
- Brain complet + **plan A** (sécurité familiale consentie « Cercle ») / **B** (coach relationnel IA WhatsApp) / **C**.
- ⚠️ **Garde-fou anti-stalkerware** posé : jamais de pistage d'un adulte à son insu (illégal/banni). À choisir/lancer plus tard.

## Mémoire (auto)
- `MEMORY.md` : pointeurs **Projet Vitrina** (LIVE) + **Exploration Relations/Famille**.
- `project_vitrina.md` (déployé/sécurisé), `project_relations-famille.md` (créé).

## Cercle — démarrage construction app (sécurité familiale)
- Décision : on construit **A « Cercle »**, **direct au MVP app Android natif**, auth par
  **vrai email** (« comme toute appli »), + « tout ce qu'une bonne app a » + idées de Claude.
- **Stack** : Expo (RN+TS) · EAS Build cloud → APK WhatsApp (pas de CB) · Supabase
  (projet `cercle`, ref `horedtmvgyorqaispldz`, schéma via MCP) · carte **Leaflet+OSM en
  WebView** (pas react-native-maps : Google Maps Android = clé + CB).
- **Vague 1 faite (tsc exit 0)** : auth email (vérif + mdp oublié), onglets
  **Carte/Cercles/Profil**, carte Leaflet live + bouton « Partager ma position »,
  créer/rejoindre cercle + invitation WhatsApp + liste live, profil (modifier nom/tel,
  déconnexion, **suppression de compte**). RLS « on ne voit que les membres d'un cercle
  partagé » = anti-stalkerware par construction.
- ⚠️ Mongazi a désactivé « Confirm email » (OK test) → réactiver vérif + SMTP gratuit avant lancement.
- **Reste** : réglages, confidentialité/consentement, icône/splash/identité, push, SOS,
  Alerte Disparition, GPS arrière-plan + notif permanente (EAS), photo profil.

### Cercle — multi-plateforme + déploiement web (14-15/06)
- Décision distribution : **Web (PWA) + APK Android** (iOS App Store = 99 $/an + CB → repoussé).
  Le même code Expo cible iOS/Android/Web.
- Carte rendue compatible web : split `LeafletMap.native.tsx` (WebView) / `LeafletMap.web.tsx`
  (Leaflet dans le DOM). Deps web ajoutées (react-dom, react-native-web, @expo/metro-runtime).
- **Version web DÉPLOYÉE LIVE : https://cercle.pages.dev** (Cloudflare Pages projet `cercle`,
  wrangler + token secrets/cloudflare.env). tsc OK, build OK, HTTP 200, clés Supabase intégrées.
- Test iPhone = ouvrir le lien dans Safari (plus besoin d'Expo Go). ⚠️ position avant-plan
  seulement sur web ; GPS arrière-plan = app Android.

## Fichiers touchés
`cercle/*` (App.tsx, src/, supabase/schema.sql, README ; hors `.env`/node_modules ignorés),
`_memoire/vitrina/*`, `_memoire/relations-famille/*`, `_memoire/journal/2026-06-14-journal.md`, `vitrina/*` (hors `.env`/`.db` ignorés), `_partage/` (captures).
