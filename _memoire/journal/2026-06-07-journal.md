# Journal — 2026-06-07

## Contexte
Lot « 5 choses qu'il reste à coder » demandé par Mongazi, dans l'ordre **2 → 3 → 4 → 5 → 1** (le #1 en dernier).
Contrainte forte rappelée : **ne jamais affirmer une info non vérifiée** (prix, règles plateforme) — confiance totale exigée. Voir mémoire `feedback_verifier-avant-affirmer`.

## #2 — Import express du catalogue (FAIT)
- `core/catalog_import.py` : `parse_products(text)` via le modèle manager → produits structurés (nom, prix, description). `_to_price` nettoie « 7 500 F » → 7500.
- Endpoint coller-un-texte → produits créés en masse (réutilise `add_products`). Session + capacité.
- UI back-office : zone « Import express » (coller la liste, prévisualiser, valider).

## #3 — Intelligence collective (FAIT)
- `core/collective.py` : `benchmark_for(merchant, min_peers=3)` → benchmark sectoriel **anonymisé** (conversion, panier moyen, top catégories). **Dormant** tant que < 3 boutiques par secteur (pas de fuite de données, honnête).
- Injecté dans le Coach commercial.

## #4 — Auto-amélioration (FAIT)
- Le cerveau d'apprentissage exploite les leçons (`lessons`) dans la génération Social + Coach (copywriting et conseils s'adaptent aux retours réels).

## #5 — Win-back automatique (FAIT)
- `db.client.list_suspended_for_winback` / `mark_winback` ; `run_winback()` dans la boucle de fond (server.py) : relance les boutiques **suspendues** (jamais supprimées) avec preuve de valeur.
- `notify.notify_winback`. MRR exclut les essais et suspendues.

## #1 — Publication automatique réseaux (FAIT, honnête)
- `core/messenger_meta.py` : `publish_facebook(page_id, message, image_url?)` (POST `/{page}/feed` ou `/photos`) + `publish_instagram(ig_user_id, caption, image_url)` (média puis publish).
- `web/server.py` : `_merchant_facebook_page(merchant)` (lookup inverse `page_merchant_*` → page_id) + endpoint `POST /api/merchants/{id}/social/publish` (session + capacité `social` + `messenger_meta.configured()` + page liée → publie la variante **Facebook** du post planifié choisi). Contexte route : `social_publish_ready`.
- UI `boutique.html` : bouton **« Publier sur Facebook »** par post (si page liée) + note honnête : *« Publication 100 % automatique sans intervention : bientôt »* (nécessite l'**App Review Meta** `pages_manage_posts` à l'échelle).
- ⚠️ Choix assumé : livrer le **vrai** publish manuel (marche dès qu'un token Page est posé) plutôt qu'une fausse boucle « auto » qui échouerait sans App Review.

## Tests
- `py_compile` OK (server.py, messenger_meta.py, db/client.py).
- Jinja parse OK (boutique.html), route `/social/publish` enregistrée, fonctions present.
- Mock : `_merchant_facebook_page` (insensible casse, page liée / non liée / sans code) + extraction texte Facebook + hashtags.

## Fichiers touchés
- `core/catalog_import.py` (nouveau), `core/collective.py` (nouveau)
- `core/messenger_meta.py` (publish_facebook / publish_instagram)
- `core/social.py`, `core/coach.py` (exploitation lessons + benchmark)
- `db/client.py` (import, winback, social posts), `notify.py` (winback)
- `web/server.py` (endpoints import / publish + `_merchant_facebook_page` + `run_winback` + contexte `social_publish_ready`)
- `web/templates/boutique.html` (import express, bouton Publier sur Facebook + note, JS `publishPost`)

## Reste / backlog
- **App Review Meta** (`pages_manage_posts`, `instagram_content_publish`) pour la publication multi-clients sans clic.
- Token Meta **permanent** (l'actuel ~24h), catch-all `*@nebula-agency.online`→gmail + IMAP, config Messenger/IG.
- Priorité business réelle : **acquisition du 1er client** (kit DM déjà livré).
