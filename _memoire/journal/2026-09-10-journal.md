# Journal · 2026-09-10

## ANGY ART prend son nom de domaine

**`angy-art.pages.dev` → https://angyart.online/**

Mongazi demande où en est la connexion du domaine. Mesuré : le nom était
acheté, sa zone **active** dans notre compte Cloudflare, ses **NS déjà
délégués**, et il servait quand même **la page de parking Hostinger**. Il
manquait les deux moitiés du branchement : le nom dans *Custom domains* du
projet Pages, et l'enregistrement DNS qui pointe vers lui. **L'un sans l'autre
ne donne rien.**

Fait par Mongazi dans le tableau de bord (aucun de nos jetons n'a `Zone · DNS`,
vérifié) : les deux noms attachés au projet, puis la racine passée de
`A 2.57.91.91` à `CNAME angy-art.pages.dev`.

Fait d'ici : `_domaine.py` (30 occurrences dans 9 fichiers, `qr-site.png`
refait **et décodé**, carte de visite et affiche réimprimées), `llms.txt`
régénéré, **224 contrôles verts**, `_dist.py`, déploiement, purge du cache de
zone, et vérification en ligne (canonical/og à la nouvelle adresse, MD5
identiques au disque, 404 sur un fichier absent, les 4 robots d'IA en 200).

### Ce que la bascule a révélé

Trois réglages qui n'existaient pas sur `*.pages.dev`, **parce qu'un
`*.pages.dev` n'est pas une zone** : le cache de zone servait encore l'ancien
HTML après le déploiement ; **le `robots.txt` géré de Cloudflare se préfixe au
nôtre et interdit exactement les robots que nous accueillons** ; et
l'obfuscation d'e-mail rend son adresse illisible sans JavaScript.

### Fichiers touchés

- `clients/11-angy-art/` : `index.html`, `sitemap.xml`, `robots.txt`,
  `llms.txt`, `affiche.html`, `_audit.py`, `_fluidite.py`, `_outils_llms.py`,
  `_build_assets.py`, `assets/images/qr/qr-site.png`, les 3 fichiers de la
  carte, l'affiche A4, `CONTEXT.md`
- `CLAUDE.md` (ligne du client 11)
- `_memoire/conversations/2026-09-10-angy-art-nom-de-domaine.md`
- `_memoire/REPRENDRE-ICI.md`

### Reste, tableau de bord uniquement

~~`www`~~ **réglé** · le `robots.txt` géré à désactiver dans **AI Crawl Control** ·
l'obfuscation d'e-mail à trancher · SPF vide + DMARC en refus.
