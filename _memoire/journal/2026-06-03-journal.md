# Journal — 2026-06-03

## Contexte
Suite Vendora. Étage 5 (prospection) livré la veille mais envoi réel bloqué : Railway bloque le SMTP → bascule sur **Resend (API HTTP)**. Vérification du domaine `nebula-agency.online` en cours (DNS Hostinger ajoutés : DKIM + SPF MX `send` + SPF TXT `send`, propagation jusqu'à 4h). Compte d'envoi = `allonebiao2@gmail.com` puis Resend (clé valide récupérée depuis NOVA).

## Documents Drive
- **Business plan Vendora** (Google Doc dans Drive).
- **Audit complet & feuille de route** (Google Doc) : ce qui marche, capacités, gaps, étapes 30/60/90j, améliorations stratégiques. 4 piliers prioritaires identifiés : 1) facturation auto, 2) WhatsApp prod, 3) sécurité d'accès, 4) réponses entrantes.

## Pilier 3 — Sécurité d'accès au back-office (FAIT)
- Code d'accès (PIN 4–8 chiffres, style Mobile Money) + **session cookie signée HMAC** 30 jours. Colonne `bia_merchants.access_pin` (hash PBKDF2).
- Garde sur la **page** `/boutique/{id}` (écran `boutique_gate.html` : créer/entrer le code) ET sur **toutes les API de mutation** (update, products CRUD, order, prospection) via `_need_session` → 401 sinon. Le front recharge vers l'écran de code sur 401.
- Endpoint `POST /api/merchants/{id}/auth` (mode set/login) pose le cookie. Réinitialisation par l'admin (`POST /api/admin/merchants/{id}/reset-pin` + bouton « Code »).
- Testé : mauvais code 401, bon code → session, actions 401 sans / 200 avec, page protégée.

## Pilier 4 — Réponses entrantes (FAIT)
- **Opt-out / désinscription** : table `bia_optouts`. Lien de **désinscription signé** unique par destinataire ajouté à chaque email de prospection (`/unsub/{token}`). **STOP sur WhatsApp** géré dans le webhook → opt-out + confirmation. La prospection **skip** les contacts opt-out.
- **Webhook Resend** `POST /api/webhooks/resend` : `email.bounced` / `email.complained` → `blacklist_prospect_email` (opt-out + statut blacklisted) pour protéger la délivrabilité. ⚠️ À brancher dans le dashboard Resend (Webhooks → URL → events bounce/complaint).
- **Leads chauds / escalade** : nouvel outil `alerter_le_patron` dans `core/brain.py` (tool-use). Quand le client veut négocier / réclame / demande le responsable → `notify.notify_hot_lead` (Telegram à Mongazi + WhatsApp patron best-effort). Branché sur le webhook WhatsApp + `/api/chat` (hors preview).
- Testé : token unsub round-trip, `/unsub` 200 + opt-out, mauvais token 400, webhook bounce → blacklist, escalade (réclamation) → alerte.

## Fichiers touchés
- `config.py` (session_secret, public_base_url), `db/client.py` (set_merchant_pin, add_optout, is_opted_out, blacklist_prospect_email), `db/schema.sql`
- `core/brain.py` (ESCALATE_TOOL + on_escalate), `core/prospecting.py` (unsub token + footer + skip opt-out), `notify.py` (notify_hot_lead)
- `web/server.py` (auth + gate + protection API + /unsub + webhook resend + STOP + escalade + reset-pin)
- `web/templates/boutique_gate.html` (nouveau), `web/templates/boutique.html` (api 401→reload), `web/templates/admin.html` (bouton reset code)

## Migrations Supabase appliquées
- `bia_merchants.access_pin`, table `bia_optouts`.

## Conversion : Paiement à la livraison + Marchandage encadré (FAIT)
- Choix Mongazi pour la facturation : **MoMo direct sur son numéro + ID de transaction, zéro commission** (pas FedaPay pour l'instant). Pilier 1 « cycle d'abonnement » reporté.
- **Paiement à la livraison (COD)** : `bia_merchants.cod_enabled`. Quand activé, l'agent propose « payer à la réception » et confirme sans preuve d'avance. `bia_orders.payment_method` ('mobile_money'|'livraison'). Alerte commande affiche le mode de paiement.
- **Marchandage encadré** : `bia_merchants.negotiation_enabled` + `negotiation_rule`. L'agent négocie LUI-MÊME dans la limite (ex: -10%), jamais en dessous, n'escalade pas pour ça. Prix négocié enregistré dans la commande.
- Cerveau : `build_system_prompt` (sections Paiement+COD et Prix & négociation), ORDER_TOOL gagne `paiement`. Agent de gestion (`manager`) peut activer ces options par ordre. Back-office : interrupteurs (toggles) Livraison→COD et Style→Négociation + champ limite.
- Testé : négociation à -10% pile (6300 sur 7000), COD enregistré (total 7300 = 6300+1000 livraison, payment_method=livraison), pas d'escalade abusive.
- Migration : `bia_merchants.cod_enabled/negotiation_enabled/negotiation_rule`, `bia_orders.payment_method`.

## Reste / à brancher
- Vérification domaine Resend (propagation) → puis 1re vraie campagne.
- Ajouter l'URL du webhook Resend dans le dashboard Resend.
- Variables Railway : `SESSION_SECRET`, `PUBLIC_BASE_URL` (sinon valeurs par défaut OK).
- Piliers restants : 1 (facturation auto MoMo) et 2 (WhatsApp production).
