# Journal — 2026-06-22

## Sessions Claude Code aujourd'hui
- **Onboarding client #05 — Saeir Thiam Bijouterie** (fiche reçue via le site, acceptée par Mongazi).
  Voir le log détaillé : `_memoire/conversations/2026-06-22-saeir-thiam-bijouterie.md`.

## Discussions Claude.ai aujourd'hui
<!-- à coller manuellement si besoin -->

## Modifications faites
- `clients/05-saeir-thiam-bijouterie/` créé (+ `assets/images|videos|docs` + `.gitkeep`).
- `clients/05-saeir-thiam-bijouterie/CONTEXT.md` rédigé (brief complet).
- `CLAUDE.md` : ligne 05 ajoutée au tableau « Clients actifs ».
- Back-office PROD `nebula-affilies` : client ajouté via `POST /api/site-lead` (lead id=4, « site »), puis passé en **En cours** dans le cockpit. 1er lead réel de la prod, aucun doublon.
- `_memoire/conversations/2026-06-22-saeir-thiam-bijouterie.md` créé.
- **Feature back-office (déployée prod)** : « savoir quel service le client a pris » → colonne `leads.service_raw` (libellé exact), endpoint `POST /api/admin/leads/{id}/service` (fixer le service retenu), affichage « Demandé par le client » + sélecteur dans chaque fiche (`server.py`, `admin.html`, `app.js`, `app.css`). Commits `50707f8`+`26a4f53`.

## Session 2 — Construction du SITE Djambar Team (pivot stratégique)
- **Plugin installé** : `uipro-cli` (npm) → skill **UI/UX Pro Max** dans `.claude/skills/` (gitignoré). Design system grounded utilisé (palette/typo).
- **Pivot** : pas une vitrine bijouterie mais le **siège d'un GROUPE Djambar Team** (bijouterie = locomotive ; comm. + événementiel/showbiz à venir). Hub **multi-pages évolutif** validé.
- **Pages créées** : `index.html` (accueil groupe), `bijouterie.html` (pôle **Saeir Thiam Bijouterie** complet), `communication.html` + `evenementiel.html` (« Bientôt »). Socle partagé **`assets/app.css` + `app.js`** (cache-bust `?v=`).
- **Assets réels intégrés** (réponses client) : logo Djambar Team 2 versions (détouré via Pillow → marque arbre seule pour la nav + favicon + apple-touch + 2 images sociales OG générées) ; **24 photos** (Colliers/Bracelets/Bagues) optimisées web (`_build_assets.py` ré-exécutable) → galerie filtrable + lightbox ; WhatsApp `2290197967671` confirmé câblé ; **Google Maps + itinéraire** (Agla Gbodjètin, près CEG L'Entente).
- **QA** : Edge headless (desktop + mobile) + mesure `scrollWidth` → **0 débordement horizontal**, tous assets HTTP 200. Hook `impeccable` : em-dashes nettoyés, `single-font` = faux positif (CSS partagé), `Montserrat` « overused » signalé à Mongazi (swap optionnel).
- **Images en chemins relatifs** (pas base64) : choix assumé pour hub multi-pages (perf + évolutivité Cloudflare Pages).
- **Mémoire** : `CONTEXT.md` réécrit (vision groupe + faits confirmés), table `CLAUDE.md` MAJ, log conversation Session 2.

## À retenir
- Un client du **site** → back-office = app **`nebula-affilies`** (`/api/site-lead`, table `leads`, `source='site'`). La base prod est sur volume Railway, **séparée** du `affilies.db` local (gitignoré).
- **Marque** : site = **Djambar Team** (groupe, logo arbre+porte) ; pôle bijoux = **Saeir Thiam Bijouterie** (enseigne physique) ; tagline **« Le Bijoutier »**. Photos déjà watermarkées (branding client) → conservées.
- **Reste** : avis clients réels + horaires, affiche **PDF A4**, validation Mr THIAM, mise en ligne **Cloudflare Pages** (sous-domaine type `djambar.nebula-agency.online` à valider).
- Visualisation locale : `python -m http.server` dans le dossier client → `http://localhost:PORT/`.
