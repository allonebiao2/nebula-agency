
## Glisser-déposer des photos produit à l'INSCRIPTION (pleine qualité)
Retour Mongazi : à l'inscription, champ photo = URL seulement (inutilisable). Demande : enlever OU vrai import d'images glisser-déposer en pleine qualité.
- **Choix** : drag-and-drop réel à l'inscription. Contrainte = l'upload exige une session (sécurité) ; or pas de session avant activation.
- **Solution** : la création de boutique **ouvre une session** (`_make_session` + cookie `bo_session` sur la réponse) → permet l'upload des photos dans la foulée (+ bonus : le commerçant arrive déjà connecté à son espace). `add_products` renvoie désormais les **lignes créées (avec id)** ; `/api/merchants` renvoie `product_ids` (ordre). 3 appelants mis à `len(...)`.
- **Front** (`onboarding.html`) : champ URL remplacé par une **zone glisser-déposer** par produit (drag&drop + parcourir, multi, aperçus + ✕, `div._imgs`), CSS `.dropz/.dz-t`. À la soumission : création → upload de chaque photo vers `/products/{pid}/photo` (FormData, **fichier brut = qualité conservée**, max 6 Mo). 
- **Testé en intégration (local)** : création → `product_ids` ✓ + cookie session ✓ + upload photo ✓ (image en galerie). Faux comptes ZTEST supprimés. Déployé.

## Glisser-déposer aussi dans le BACK-OFFICE (photos produit)
Demande Mongazi : même glisser-déposer + import dans le back-office.
- `boutique.html` carte produit : input file nu remplacé par une **zone `.bdz`** (drag&drop + clic « parcourir », multi). JS refactoré : `uploadFiles(fileList, pid)` (cœur, filtre images + max 6 Mo) + `bdzDrop(e,pid)` ; l'input et le drop appellent le même upload immédiat (produit déjà existant + session). CSS `.bdz/.drag`. Plus aucune réf à l'ancien `uploadPhoto`. Jinja OK. Template-only → local le sert en direct.

## Audit outils/coûts/ROI + analyse Vendora + corrections F1/F2/F3
Demande Mongazi : ① présentation animée des coûts de chaque outil + ROI + projections ; ② analyse complète de Vendora (forces/faiblesses/erreurs internes à corriger). Déclencheur : Netlify n'était PAS gratuit illimité (reproche justifié) → exigence de prix **vérifiés**.

**Livrables (`_audit/`)** :
- `presentation-couts-roi.html` : deck animé autonome (12 slides, motion design pur CSS/SVG). Prix ✅ vérifiés en ligne (juin 2026) : Railway 5$/mo, Hostinger VPS 5–12$, Netlify gratuit plafonné→20$, Supabase gratuit (pause 7j)→25$, Meta WhatsApp réponses client gratuites, FedaPay 1,6%, Cloudflare Pages bande passante ILLIMITÉE gratuite sans CB (= fix Netlify), Claude Opus 4.8 5/25$ · Sonnet 4.6 3/15$ · Haiku 4.5 1/5$ par M tokens. Plancher actuel ≈ 6 600 F/mois, rentable dès 2 boutiques. 1$ ≈ 600 F.
- `analyse-vendora.md` : verdict ≈ 8/10. Forces : séparation des rôles, grounding, gating serveur. 3 chantiers = tous liés au COÛT.

**Corrections codées + testées + DÉPLOYÉES (commit 4fa9a4a, push main, railway up boutique-ia)** :
- **F1** — bug cache : l'heure à la minute était DANS le bloc système mis en cache (copilote patronne `assistant._system`, fondateur `_admin_system`, `support._system`) → cache invalidé chaque minute. Fix : 2 blocs système (stable caché + date volatile NON cachée via `_now_fr()`). Le vendeur `brain.py` n'avait pas le défaut.
- **F2** — routage modèle par message : `model_config.model_for_vendeur(history)` → Haiku par défaut, modèle configuré dès signal d'achat/négo/conv engagée (regex `_BUY_RE`). Interrupteur `vendeur_smart_routing` (défaut ON). Branché `brain.reply`. Testé hors-ligne (bonjour→Haiku, « c'est combien »→Sonnet).
- **F3** — mesure coût : `core/usage.py` (prix + calcul cache) + table **`bia_usage`** (migration Supabase appliquée projet anqdxz…) + `record_usage`/`usage_summary` (coût USD+FCFA + taux de cache par tâche/boutique). Télémétrie sur vendeur/copilote/fondateur/gestion/support. Guardé (no-op si table absente). Coût mesuré : ~7 F/tour Sonnet, ~2 F Haiku.
- Reste backlog (analyse §5) : F4 idempotence commande, F5/F7 seuils tokens, F6 tri capacités, F8 découpe server.py.
